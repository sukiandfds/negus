# negus 总产品定义

更新时间：2026-09-14 01:03 +08:00

## 文档职责

本文件是 negus 当前有效产品定义的总入口。它只描述现在认可的产品行为、核心逻辑、归属关系和边界，不保存已经废弃的方案。任何功能修改都必须同时更新本文件对应章节和该功能的 `FEAT-*.md`；实现状态以功能状态索引为准，历史过程放在功能更新记录中。

固定阅读顺序：`PROJECT.md` -> `PRODUCT_DEFINITION.md` -> 本次涉及的 `FEAT-*.md`。

## 产品定位与核心卖点

确认记录：2026-09-14 01:03 +08:00，`PM-002`。本节定义产品方向；实现状态仍以各功能记录和真实验收为准。

**Negus 是一个可以用自然语言不断定制的个人／企业工作空间。** 用户在浏览器中提出需求，让 Negus 持续创建、调整和连接自己需要的功能页面。

三个核心卖点：

1. **能联网，有浏览器就能用。** 用户从手机、平板或电脑进入自己的工作空间。实际访问依赖工作区服务在线、网络可达和有效授权；浏览器是访问入口，后台仍需要运行环境。
2. **随时随地，按需求定制页面。** 用户告诉 Negus 想增加什么、放在哪里；完成开发、验证与所需发布后，即可使用新的每日资讯页面、项目进度管理页面或进销存系统入口，并继续用自然语言调整。
3. **任何功能，任何位置。** 产品允许围绕实际工作扩展功能和入口，例如员工首页的报销发票上传、项目中的会议预约面板，以及员工之间的 AI 助手按授权通信。这里表达可持续扩展的方向，具体能力受数据、权限、接口和实施条件约束。

### 用户体验与工作流程

- 原有定位着重浏览器查看／控制 Codex、项目群聊与交付物；现在进一步明确：用户可以持续改变工作空间本身的功能和组织方式。
- 目标流程：描述用途与位置 -> 确认必要的功能和权限范围 -> 创建或修改 -> 查看预览与验证结果 -> 在授权范围内发布 -> 从选定入口使用 -> 后续继续调整。
- 页面应连接真实数据和业务流程。新增“进销存入口”不等于已经实现库存管理；发票上传也不等于已具备识别、财务审核或支付能力。
- 开发、重启和部署由系统协助处理，用户主要关注需求、预览和上线结果。不承诺固定生成时间或所有变更都必须重启；不中断其他工作、可恢复发布是后续设计目标，尚非已验证保证。

### 归属、现有基础与未实现项

- 新增页面、业务文件和数据归对应工作区／工作项目；页面放置位置不改变其数据归属和访问权限。
- 员工会话继续遵循下文既有归属规则。员工间 AI 通信只共享授权的工作事项，不能默认读取他人的私聊或取得其操作权限。
- 当前已有真实 Codex 执行、浏览器/PWA 入口、项目状态、HTML/PDF 交付物和项目内 Agent 协作等基础；这些实现的验收状态仍分别维护。
- 自然语言定制并发布到任意指定位置的一体化流程、跨功能数据复用、正式多人权限和跨员工助手通信仍需具体设计与验收。现有项目内多个 Agent 不等同于已完成多名真人各自 AI 助手之间的通信。
- 每日资讯、进销存、报销和会议预约是代表性用途，不是已交付的内置业务系统清单。定时运营仍须按具体任务配置与验收。

### 发展与宣传方向

优先展示一条真实的“提出需求 -> 功能上线 -> 实际使用 -> 再次调整”链路，逐步验证功能能复用已有数据、账号和流程，更新后原有业务仍可用。以实际节省的操作、维护时间和交付质量验证商业价值。

宣传主句：**Negus：你需要什么工作功能，直接说出来。** 用户关于 AI 改变传统 SaaS 的观点作为战略判断保存，不写成已经证实的市场结论；对外以具体功能与演示证据说明价值。当前不限定为内容团队专用产品，也未确定收费模式。

本次仅更新文档，未改变代码、运行方式、部署和已有功能状态。用户原话与更新记录见 [`PM-002`](docs/project-management/items/PM-002/item.md)，宣传稿见 [`docs/promo-copy.md`](docs/promo-copy.md)。

## 全局产品原则

2026-09-25 02:27 +08:00 BUG-20260925-01 / FEAT-001 (handoff_pending): 修复切换对话时正文和执行状态混用、旧发送回执和队列请求覆盖新对话。模型应用请求按 Thread 隔离，发送准备期切换对话时不把消息改投新对话。消息合并和显示复用同一顺序函数，同轮优先原生条目顺序，删除显示层十分钟排序限制和重复循环；备用分页保留已有助手时间。未新增依赖或改写历史记录。内存定向回归、3项历史测试及前端构建通过；未重启、未提交，浏览器实际场景和存量串话记录未验收。

2026-09-22 01:51 +08:00，加载修复续查：项目状态面板只在可见页面读取，同一面板最多一个在途请求，10秒超时后保留旧内容并允许后续重试；相同结果不替换展示状态。群聊切换清零已取消的流式绘制标记，旧事件连接不再更新新连接状态或内容。10项加载回归测试与构建通过；单次本机只读测量目录1244ms、自动化4ms、群聊快照1ms，不代表手机、公网或视觉验收，整体仍在进行。

2026-09-22 01:48 +08:00，FEAT-002 加载修复：群聊切换后旧快照、历史分页、错误和导航结果不得覆盖新群；快速切回优先恢复离开前已保存的最新消息快照。历史日期页仍沿用原规则不持久化。群聊和员工归属、发送与执行不变。代码及定向测试通过，浏览器视觉与实际读取速度未验收。

1. 项目、员工、群聊和会话是不同对象，不能根据当前页面、文件路径或 Thread 自行猜测归属。
2. 员工是长期独立主体，拥有自己的员工项目、直属单聊和参与各项目群聊的长期会话。
3. 工作项目保存项目文件和公共群聊记录；员工可以按授权访问工作项目，但不能在工作项目的普通会话列表中创建或留下员工会话。
4. Runtime Thread 只是执行引用，不决定员工身份或产品归属。所有归属必须由明确的项目和会话绑定表示。
5. 用户可见结果必须来自真实运行数据。定义完成、代码完成、构建通过和用户验收是不同状态。

## 当前功能总览

| 功能 | 当前产品定义 | 详细文档 |
| --- | --- | --- |
| 单人 Codex Web 对话 | 查看和继续真实 Codex Thread，支持发送、追加、停止、恢复、历史、执行过程和 Codex 补充提问；网页不另造一套假会话 | `FEAT-001` |
| 项目群聊与多 Agent | 工作项目拥有公共群聊；员工在自己项目下用独立长期会话参与群聊，结果只生成一次并发布回群聊 | `FEAT-002` |
| 附件和内容渲染 | 对话和群聊可以携带真实图片、音频、视频和文件，并按统一内容块展示 | `FEAT-003` |
| PWA、设备和移动端 | 用户可以从电脑和移动设备进入同一产品；更新、设备状态和连接状态必须可理解 | `FEAT-004` |
| Desktop/Web 连续性 | Desktop 与 Web 可以读取同一持久化 Thread，但不得把可读取误报为已实时同步或可安全同时写入 | `FEAT-005` |
| 公网入口和访问控制 | 远程入口应稳定、受保护并可恢复；当前可用性与长期可靠性分别验收 | `FEAT-006` |
| Agent 交付物 | Agent 结果可以登记为可预览、下载、迭代和审核的 Artifact | `FEAT-007` |
| HTML/PDF 交付 | Agent 可以生成并交付真实 HTML 和 PDF，预览、下载与审核状态保持一致 | `FEAT-008` |
| 远程开发主机 | 在不破坏现有服务和远程连接的前提下检查、构建、准备重启和恢复项目 | `FEAT-009` |
| Orca Runtime 试验 | 仅保留为暂停的技术试验，不属于当前产品主路线 | `FEAT-010` |
| 用量监控 | 在不影响对话的情况下展示真实供应商用量摘要和详情，失败时保留可解释状态 | `FEAT-011` |
| 消息复制 | 用户可以复制真实消息内容，并在浏览器权限失败时获得明确反馈 | `FEAT-012` |
| 从消息分叉 | 从指定已完成消息调用原生 Thread 分叉并进入可继续的新会话 | `FEAT-013` |
| 会话归档 | 普通会话可以归档、查看和恢复，归档不等于删除 | `FEAT-014` |
| 生图与自动化 | 自然语言请求进入真实 Codex Thread/Turn，由模型调用图片能力；工作台、批量和定时属于后续范围 | `FEAT-015` |
| negus 产品身份 | Web、PWA、仓库和主要界面统一使用 negus，升级不应静默丢失历史数据 | `FEAT-016` |
| 多业务项目与会话归类 | 普通项目会话按业务项目归类；员工会话始终归员工项目，目标工作项目只提供文件和群聊工作范围 | `FEAT-017` |
| Codex 原生 Goal | Negus 只提供能力入口并复用 Codex 原生 Goal 流程，不维护第二套 Goal 生命周期 | `FEAT-018` |
| 项目记忆入口 | 项目管理页显式读取最近更新、关键决策和公共群聊完整历史，并说明哪些新增内容进入 Agent 上下文；不引入隐式长期记忆 | `FEAT-021` |

## 桌面入口

2026-09-22 01:43 +08:00，FEAT-023 / FEAT-001 加载体验修复中：已有任务内容在后台检查时保留，结果相同时不重置展示；检查时间独立保存，避免刷新后重复查询。普通会话首次同步后有限串行预读，切换优先显示本会话缓存，旧会话响应只更新自身缓存；截断快照必须补取完整页，无缓存时显示加载状态并清除上一会话错误。功能隐藏不清空同一会话的定位标识。数据归属、发送和后台执行不变。5项加载测试与前端构建通过，实际闪屏、读取速度和群聊切换仍待验收，不能据此认定全面修复。

2026-09-22 14:06 +08:00：桌面浏览器与手机、触控平板共用同一套外壳。隐藏最上方模拟窗口菜单栏及其高度占位，保留下方标题与功能栏。对话侧栏一律为抽屉，默认收起；标题栏原有按钮打开，点击遮罩关闭。宽屏仍保留标题栏布局，不再为鼠标桌面单独保留常驻侧栏。

2026-09-22 00:48 +08:00，FEAT-023：定制桌面/完成定制入口位于固定顶部栏分享按钮左侧，以图标切换；桌面上方不再显示操作提示或独立工具条。画布无整体蓝色焦点外框。定制模式左右各18px保留纵向滑动安全区，绘制从中间开始，组件仍可拖放到两边，布局坐标不缩窄。右侧采用强制滚动槽与灰色细滚动条，iPad常显行为待真机确认。异常反馈不占顶部布局。

2026-09-22 00:24 +08:00，FEAT-023 当前交互修正（取代下文旧的形状选择和同步失败提示规则）：画完由本地识别直接生成圆形或矩形，不询问形状。任务读取短暂中断保留记录，自动重试，以更新时间说明数据新鲜度，不用错误横幅打断浏览。自动化部分来源不可读时保留已有任务，完整读取成功后再更新完整列表。真实任务执行失败、消息未发送或布局未保存仍准确提示，不伪装成功。此次改动覆盖桌面组件，未宣称全系统故障恢复已完成。

2026-09-21 02:08 +08:00，FEAT-023：当前任务的刷新操作放在卡片与详情标题栏，以图标按钮呈现，替代正文中独占一行的“重新同步”。沿用现有数据重试逻辑，读取期间禁用；不改变数据来源和状态判定。

2026-09-21 01:34 +08:00，FEAT-023 首页状态修复：正常首页只呈现业务结果，不铺设模糊免责声明。失败显示具体故障、历史快照标识与重试入口，加载/失败时不推断没有运行任务；自动化失败展示快照读取时间。数据来源和管理说明放在详情折叠区。此变更不改变任务真实状态和数据覆盖范围。

2026-09-20 20:54 +08:00，`FEAT-023 v0.3.0`。桌面是持续展示真实业务功能的工作空间，聊天用于指挥和调整功能，不能把解释或最终回复自动塞入组件冒充功能。沿用侧栏、顶部、原 ConversationComposer、桌面 Thread 与助手回复浮层；针对组件对话显示目标名称及位置上下文，回答仍在原对话内。任意新业务功能仍需开发，未接入语音、跨设备布局同步或自动生成应用运行容器。

2026-09-22 00:12 +08:00，FEAT-023 v0.4.0：仅保留“定制桌面／完成定制”切换入口，移除框选和撤销/重做按钮、普通模式常驻调整按钮。定制模式禁用文字选择，点击组件选中、直接拖动、手柄缩放，更多菜单提供针对区域对话、固定/取消固定与移除。空白处用连续画线代替拉框，实时笔迹、本地识别圆形与长方形，松手纠正形状；识别不明确时提供两种形状选择，点击/直线/未闭合手势不创建。画完保存并选中，输入框关联区域，不发消息、不调用模型、不启动开发。形状不限制内容，圆形中也允许用户要求表格，后续如何生成内容与整体技术分层另行讨论。布局仍为12列、32px行高、本浏览器持久化；圆形显示保持正圆。移除不停止后台业务。助手恢复历史能力不在本轮实现范围。

左侧「当前任务」汇总真实目录会话、执行状态和原生 Goal：最多 3 条标题、最新用户指令、状态；运行优先，否则显示明确完成项，不把 idle 当完成。放大、分类列表和原会话导航保持；浏览器缓存先展示、后台更新，流式事件不重复拉全部详情。长期 active Goal 在轮次间保留；失败、暂停和额度用完分别展示。未接入的数据源不能保证被发现。

右侧「自动化任务」读取主机 Codex 调度数据库和 automation.toml 配置，展示真实名称、启停状态、周期、下次执行；点开查看全部/启用/其他状态、最近触发、已有执行记录、通知方式及关联会话。数据库只读，配置按 ID 合并，不读取或返回任务 prompt；不运行任务、不调用模型。可见页面每 15 秒刷新，后台页面暂停读取，返回刷新；失败保留缓存并明确警告，不以空列表声称没有任务。已启用不代表正在执行，无运行明细明确标注。范围仅本机 Codex 自动化，不包含操作系统计划任务、其他设备或外部服务；暂停/恢复/修改计划仍需在 Codex 自动化管理操作，本组件暂不提供写控制。

已识别的右上角「选区 1」旧自动化说明预览自动迁移为真正的自动化组件，其他已保存内容保留；新选区直接用于描述功能需求，不再自动接收聊天回复。详细变更、验证及服务加载状态以 FEAT-023 为准。

## CC Switch 共享渠道

2026-09-25 01:57 +08:00 FEAT-022: 配置列表与编辑页复用同一份倍率；当前运行配置保留 CC Switch 对应条目的倍率，Grok 使用实际 Key 加入现有供应商查询和缓存，刷新时重读，无新增配置项。修改前实际只读查询确认 Grok 0.10×、当前 GPT 分组 0.15×；这是查询时的供应商值，不写死在页面。未重启服务，页面显示待真实验收。

2026-09-25 01:47 +08:00 FEAT-022：模型配置行为对齐（handoff_pending）。之前新对话可能沿用运行环境思考强度，发送失败后无上次成功选择可恢复。现在新对话使用本浏览器默认配置、模型和思考强度，电脑与手机不同步默认值。手动选择先在当前页保留，下次发送才应用；不支持原强度时复用现有支持列表自动选择。发送前应用显示选择，真正 turn/start 显式传入模型和强度；失败保留当前选择并报错。复用现有会话路由记录保存成功完成轮次的选择，刷新后恢复，无成功记录则用本浏览器默认值。历史会话复用原生 rollout 读取上次回复的模型和强度，不新建恢复文件或系统。手动刷新模型失败保留列表并显示错误。配置切换仍使用该配置已有默认模型，不扩展 CC Switch 管理。未重启服务，真实浏览器和供应商发送未验收。

2026-09-24：输入框、新建对话和配置管理中，用户可见的「渠道」改为「配置」，「默认分组」改为「默认配置」。用量里的价格分组名称不变。这只改显示名称，不改变选择、发送或保存逻辑。

2026-09-22 02:08 +08:00，FEAT-022：外部渠道的手动模型刷新查询该渠道真实/models目录，新增模型进入所选渠道的可选列表及会话路由，缓存可跨服务重启恢复；失败保留列表并报错，不变更当前会话模型或触发生成。当前Codex渠道继续使用原生模型目录。真实查询确认Grok 4.7，代码测试与构建通过，后台尚待重启加载。

2026-09-22 02:03 +08:00，FEAT-022：模型选择框右侧提供小刷新图标，手动重读渠道模型目录；读取期间禁止重复点击，当前对话模型选择保持不变。构建通过，手机视觉待验收。

2026-09-22 01:59 +08:00，FEAT-022：渠道列表包含已配置的当前Codex、Fusheng Grok及CC Switch渠道；选择仅作用于当前对话，模型下拉继续按所选渠道过滤。列表使用本页面60秒内存缓存与请求合并，后台更新不阻塞已有选项，刷新按钮强制重新读取；打开模型菜单不重复触发目录更新。内置渠道不提供CC Switch编辑，不新增Key或改全局模型。构建及模型目录只读验证通过，真实生成与手机视觉待验收。

2026-09-18 18:49 +08:00，`FEAT-022 v0.1.3`：模型菜单仅显示当前渠道目录中的模型，去除重复项与渠道名称前缀；渠道独立一行显示与切换。选择模型不改变渠道。尚未查询完整供应商模型目录，仅有默认模型的渠道不凭空补全模型。

2026-09-18 18:12 +08:00，`FEAT-022 v0.1.2`：一级列表直接切换渠道，新增/编辑在二级。渠道选择立即保存，当前会话编号、标题、侧栏条目和历史不变，不发送历史聊天消息或触发回复；下次用户发送时，将完整原生 rollout 放入目标运行目录，thread/resume 恢复同一个 Thread ID，校验目录与编号后仅发送新消息。该规则取代此前普通 GPT/Grok 跨渠道新建会话及自动发送历史的定义。失败不发送、不另建；完全空白尚未落盘会话暂时提示无法切换。共用 CC Switch 数据库，写入前备份并保留未知字段及当前选择，Negus 会话切换不改变电脑 Codex 全局渠道；CC Switch 当前启用渠道需先在桌面切走再编辑。倍率优先读取供应商分组与 pricing，实际 Key 查询 token 名称并排除同名多分组歧义；不能核实时显示配置倍率。顶部倍率随所选渠道变化，未知显示 --。仅接入 responses API Key 渠道，不接入 OAuth/自定义认证。共享库沿用 CC Switch 密钥存储方式，浏览器不回显 Key，运行副本使用 DPAPI。外部 Desktop 并发协调、桌面即时刷新、手机和真实生成待验收；23 项测试、构建和本机原生同编号恢复验证通过，安全重启等待当前 Turn 完成。详细边界见 `FEAT-022-cc-switch-channels.md`。

## Codex 补充提问

更新时间：2026-09-18 11:14 +08:00，`FEAT-001`。

Codex 在普通会话、外部模型渠道会话或员工直属单聊中发出 `item/tool/requestUserInput` 时，Negus 保留原请求并在所属会话显示互动弹窗。弹窗按 Codex 原协议展示最多三个问题、选项及说明，支持“其他”回答和秘密输入；用户提交后，答案只返回原运行实例和原请求，当前 Turn 继续执行。刷新、断线恢复或切回会话时，仍从服务端读取尚未回答的请求；运行实例重启后不把失效请求伪装成可继续。

当前不包含命令、文件修改或权限审批，也不包含 MCP 通用表单、URL 验证和群聊内 Agent 的互动弹窗。这些属于相邻能力，需分别确认安全边界和承载页面后再开发。

## 输入区工具箱交互

2026-09-18 14:01 +08:00 上下文显示补充：输入区右下角紧凑显示当前使用量、Codex 当前会话实际可用窗口和使用百分比，例如“上下文 42k / 258k（16%）”；自动压缩百分比及对应 k 数放在展开菜单内。容量只使用 Codex 运行时 `modelContextWindow`，不把供应商宣传的理论上限当成当前可用值。切换模型后先清空旧数据，收到新模型 Token 事件后再显示；自动压缩直接比较真实 Token 数和阈值 Token 数，不使用四舍五入后的显示百分比判断。

2026-09-17：手机侧边栏展开时，功能切换按钮位于遮罩下方。归档/普通列表切换复用本页缓存，连续点击合并为最后一次加载；缓存随后用服务端结果更新。模型切换直接使用服务端确认结果并阻止先前请求覆盖；新会话优先使用本浏览器最后一次成功选择的模型，旧会话仍使用各自设置，不强制统一或跨设备同步偏好。

2026-09-16 23:43 +08:00 样式补充：保留输入区外层定位、宽度和功能顺序；手机端统一文本和工具栏边距，收紧操作行与设置行间距，模型箭头紧邻名称，设置行用轻分隔线区分层级。发送按钮保留触控尺寸，缩小亮色视觉面积；单聊和群聊采用一致处理。

更新时间：2026-09-16 23:30 +08:00，`FEAT-001` / `FEAT-002`。单聊和群聊工具箱按钮切换展开与收起，点击外部或 Esc 关闭；打开时不主动聚焦输入框，选择命令后再进入输入。按钮以展开状态高亮。保持现有位置与布局，附件、工具箱、发送控件统一使用既有圆角变量和图标规格；菜单使用全局面板配色且能接收点击。仅调整前端交互，不改变命令执行和数据归属。

## 会话自动标题

### 普通会话跨模型渠道切换

2026-09-18 00:07 +08:00 修复：Grok 4.5/4.6 的 low/medium/high 已通过实际渠道请求验证，模型菜单开放低/中/高，跨渠道时所选等级作用于新会话。空会话无需读取持久化历史或发送空上下文；已有历史在原生读取不可用时使用所属渠道的 JSONL 兼容读取。新建空会话仍保留侧栏条目。同一轮追加指令按消息原始序号排序，不把所有助手消息时间写成轮次结束时间；前面的回复折叠为“追加指令前的回复”，真正中断的轮次单独标注。原始记录不删除。25 项定向测试、前端构建及实际问题会话的 JSONL 顺序检查通过；手机验收待完成。

2026-09-17 23:39 +08:00，FEAT-001：普通输入框列出已配置 Fusheng Grok 4.5/4.6。GPT 与 Grok 相互切换时在原模型设置浮层提示新建会话并携带上下文，确认后同项目创建独立会话；原会话保留。同渠道切换继续原会话。每个会话单独保存模型与渠道，侧栏对已知模型显示统一 GPT/Grok 小标识。普通 Grok 会话与员工会话的身份、目录归属保持分离，复用现有独立供应商运行环境。

携带完整可读取的文字消息，不转交附件二进制，不把历史指令当成新任务。超过 100000 字符或历史不完整时明确拒绝，不静默截断。新会话首次发送仅要求确认接收；发送未确认时原会话保留，并提示检查已创建的新会话，禁止自动重发。Grok 未核实分组倍率时显示未知，不沿用 GPT 分组倍率；账户用量弹层仍为已有账户统计，不代表 Grok Key 已单独统计。Grok 4.6 已通过本机 Codex 临时会话真实回复；33 项定向测试通过，手机完整操作与后台加载新代码待验收。

最近对话与每日进度补充（2026-09-17 22:46 +08:00，FEAT-001）：摘要支持现有供应商凭据及 env_key，不再强制依赖旧 auth.json；保留独立摘要模型 gpt-5.6-terra，失败不升级到主对话模型。保留当前入口及按需增量整理，失败保留旧内容并提示重试。04:00 仅为日期分界。持久化与未来“桌面”不在本次范围。

2026-09-17 22:57 +08:00：状态面板增加“检查模型”弹窗。用户输入 Fusheng API Key 后，通过已授权的后台向固定渠道查询模型列表；Key 不写入文件、浏览器存储或聊天，提交后清空输入框。结果只代表渠道列出的模型，不表示实际生成测试通过或已确认价格，不自动修改摘要模型。后台接口需重启后生效。

更新时间：2026-09-16 23:21 +08:00，`FEAT-001`。手动名称优先，其次使用 Negus 本地一次性自动概括；没有自动标题时使用会话首条消息预览，空会话显示短编号。历史列表不批量调用模型，不定时改名。新消息仍触发现有后台概括，等待上限调整为 60 秒，失败保留预览、下次发送可重试；不写入桌面端名称。预览是消息摘录，不保证概括完整长会话。

## 工作区访问与分享

更新时间：2026-09-16 23:15 +08:00，`FEAT-020`。

标题栏统一使用原有“分享”入口，复制带授权的当前页面链接或展示二维码，不再单设“内测访问入口”。链接仍进入同一个工作区，沿用共享令牌和 Cookie 授权，不创建独立账号或成员权限。相关数据归属、访问范围和后端授权逻辑不变；正式账号及细粒度权限尚未实现。

## 项目群聊与员工会话

### 发言和点名

2026-09-22：普通发言只写入公共群聊，不叫醒任何员工，也不生成决策。只有消息里的 @员工 会让被点名的人按出现顺序工作。没有点名时，输入区说明已经记下、不会有人回复，不额外插入助手消息。明确传入的员工名单仍视为点名。 员工第一次被点名时，只把最近少量群消息放进他的会话；之后每次只补充他上次回复后的新消息。引用某条消息时，公共记录和员工上下文都保留这是在回复谁。停止、失败或超时必须在群里留下清楚状态，不能看起来像已经完成。

2026-09-22：同一个人被连续点名时，先发出的那一条先做。这一轮只看到自己这条点名，以及下一条点名之前的补充；后一条点名留到下一轮，不算已经看过。同事在回复里点名交接时，被点到的人接的是那条交接，不会把用户更晚单独点名的另一件事提前做掉。窗口装不下时，只说明开头没放进来的条数；中间断掉的地方另行标明，不把两处省略说成同一件事。过长的单条在员工提示里截断，并说明全文仍在群里。

2026-09-22 03:12 +08:00：员工看到的上下文以这次点名为中心，并按群里的发生顺序阅读。点名那条消息即使被后面的闲聊挤出最近窗口，也会单独保留；它引用的原消息如果不在这次窗口里，会附上公共记录中的原文和附件，不用输入框里的短摘录。没有单独引用、而且上一条确实紧挨着时，短的续话、纠正和加强沿着那一条理解，不另换对象。窗口里前后消息序号不连续时，标明中间省略了多少条，不把它们说成紧挨着发生。这条消息里已经点到的其他员工会自己回复，当前员工不为了交接再点一次。自己的消息发送失败会留在群里，可以原样重发。公共记录仍然保存全文。员工会话一旦接受这次任务，就把这批消息记为已交付；启动失败不推进，避免同一段内容反复进入长期会话。前面已经有任务时，不把正在工作的员工改成排队，输入区说明被点名的人会在当前任务结束后按顺序继续。

翻看更早记录时，新的发言和正在回复不会插进那一页。这时发送或重发会先回到最新。有新消息时，回到最新的按钮写明条数。在最新一页往上翻时，只要离开底部，就显示回到底部；期间新到的消息按条数标出。

同一条消息点名多人时，先被点到的人先工作，其余人显示“等候上一位”。前面还有别的任务时，这批人显示“等候前面的任务”，正在工作的人状态不变。顶部状态把正在工作和等候的人分开写，不把等候说成正在工作。

输入框里已经点到员工时，发送前写明回复顺序。消息上多人点名写“按顺序点名了”。失败消息里的原任务标成“未完成的任务”，发送进行中不能连点重试。

还没轮到的员工不写进历史记录。消息列表底部只临时显示他们正在等候上一位或前面的任务，轮到之后这行消失。

正在翻旧消息时不显示这行等候，避免把它看成当时的历史。员工还在工作时，即使输入框里已经有下一条内容，工具栏也保留停止。

同一时刻员工正在工作时，输入框为空则主按钮变为停止，有内容时发送会加入等候。停止会结束当前任务并取消还没开始的排队。员工真正开始处理后，等候说明消失。输入区可以查看员工上下文规则：完整记录留在群里，员工只接收尚未交给他的新消息，点名和引用单独保留。

消息列表上方列出当前群里的员工。每个人显示空闲、等候或正在做的事；点一下就把 @名字 放进输入框。名字使用员工的正式名称，保证点名能路由到本人。

点引用摘录会回到原消息：已经加载的直接滚到那一条并短暂标出；不在当前页时按原消息序号打开周围记录。引用会记下原消息序号。更早、没有序号的引用如果当前页里找不到，就说明无法定位，不假装跳转成功。

还在发送中的消息不能引用。如果引用时它还没确认，等确认后改成真实消息编号再发出；来不及确认就提示稍后再发，避免引用被悄悄丢掉。

成员消息里的 @员工 会高亮。员工启动失败、超时或没有产出时，系统消息保留失败标记，并链到当时那条点名。再试一次会重新点名，同时引用原任务；原任务不在本次窗口里时，员工仍能看到原文。用户主动停止的消息不提供重试。

### 对象和所有权

- **工作项目**：拥有项目文件、项目群聊和完整公共群聊记录。
- **员工项目**：始终拥有该员工的直属单聊，以及该员工参与每个项目群聊所使用的长期会话。
- **员工直属单聊**：用户直接与员工沟通的普通长期会话，始终属于员工自己的项目。
- **员工的项目群聊会话**：一个员工在一个项目群聊中的普通长期会话。`一个员工 × 一个项目群聊 = 一个长期会话`，始终属于员工自己的项目。
- **公共群聊记录**：保存在工作项目中的完整聊天记录，可以包含员工公开发布的消息，但它不是员工的 Runtime 会话。

员工可以访问其参与群聊对应工作项目路径下的文件，并按任务读取、创建、修改或删除文件。访问目标项目不改变会话所有权，也不得让员工 Thread 出现在目标工作项目的普通会话列表中。

### 第一次参与群聊

员工第一次被某个项目群聊调用时，在员工自己的项目下创建该群聊对应的长期会话。它像普通 Codex 新会话一样开始，默认几乎没有聊天上下文，可以读取目标项目根目录中的项目说明等文件，但不自动灌入该群聊此前的全部历史。

工作项目必须保存完整群聊记录。用户要求员工查看最近或历史群聊内容时，员工可以通过目标工作项目访问这份记录。首次调用未来如何结合每日压缩结果，尚未定义。

### 后续调用和增量上下文

同一项目群聊以后再次调用同一员工时，继续员工自己项目下的同一个项目群聊会话，不新建会话。系统把该员工上一次回复之后到本次调用之间新增的群聊内容按原顺序补入上下文，使员工理解两次参与之间发生的讨论。

每日压缩、长期摘要、通用经验、项目经验和雷点沉淀属于后续能力，不作为当前已经实现的功能。

### 回复生成和公开发布

用户在工作项目群聊中指定员工完成工作后，实际运行发生在该员工自己的项目下面对应群聊的长期会话中。员工可以操作目标工作项目文件。员工完成后，该长期会话生成的最终回复原样发布到工作项目群聊，员工会话和群聊展示的是同一份结果，只生成一次，不重新总结、不二次生成。

工作项目保留员工公开说出的群聊消息；员工项目保留员工用于持续工作的真实会话。两者可以引用同一份结果，但不能因此把员工会话归到工作项目。

### 停止、失败和过程输出

当前最低要求是中止或失败具有清晰状态，不能伪装成已完成。把有价值的过程文本带入群聊属于后续开发：正常执行可展示阶段性说明；中途停止时，优先保留任务理解、停止前最后一段有效文本和明确停止位置。是否展示命令和其他底层过程以后单独定义。

### 归档和退出

当前项目群聊归档、删除或员工退出时，不自动删除、迁移或重新归属员工自己项目下的对应群聊会话。后续生命周期规则另行讨论。

### Codex Desktop 混入员工会话：现象、调研与暂缓决定

记录时间：2026-08-17 12:48 +08:00

#### 问题现象

用户只在 Codex Desktop 中打开 negus 主仓库，没有单独打开任何员工项目，但 Desktop 的 negus 项目会话列表中出现了由 negus 群聊创建的多个员工会话，表现为不同员工/角色各自占据一个普通项目会话。这个现象违反“员工会话始终属于员工自己的项目，工作项目的普通会话列表只显示工作项目会话”的产品定义。

当前已抽查到的员工 Thread 元数据示例为：

```text
threadId = 01a00060-a6d0-7fd3-a309-3ceb89768692
cwd = D:\codingproject\codex-collab-panel-demo\employees\manager
originator = Codex Desktop
source = vscode
```

因此，问题不是员工目录或会话被物理存放到了目标工作项目，而是 Codex Desktop 把位于同一 negus 仓库子目录、且来源标记为 `vscode` 的员工 Thread 归入了 negus 项目会话列表。

#### 已验证的调研结果

1. Codex 官方配置目前没有“忽略某个子目录中的会话”选项；`.gitignore` 只控制 Git 文件跟踪，不能控制 Desktop 会话列表。
2. Codex App Server 的 `thread/list` 支持 `sourceKinds` 和 `cwd` 过滤。官方文档说明：未指定 `sourceKinds` 时只返回交互来源 `cli` 和 `vscode`；可识别的非交互来源包括 `appServer`。
3. negus 已在 App Server 初始化时声明自定义客户端身份 `clientInfo.name = "negus"`，并在员工 Thread 中传入 `serviceName`，但现有员工会话元数据仍然是 `originator = Codex Desktop`、`source = vscode`，这些字段没有阻止 Desktop 展示员工会话。
4. 当前 Codex App Server 协议存在 `sessionStartSource`。给员工 `thread/start` 传入 `sessionStartSource: "appServer"`，是目前找到的最小原生排除方向；`threadSource: "system"` 不是同一维度，不能代替会话来源分类。
5. 该方向存在明确产品代价：员工 Thread 可能不再出现在 Desktop 的普通会话列表中。用户以后即使单独打开员工项目，也可能无法从 Desktop 侧栏直接找到并继续 negus 创建的原会话，只能继续由 negus 按 Thread ID 读取和恢复。因此本轮不采用这个方案。
6. 官方 Desktop 已支持把仓库子目录保存为本地项目，也支持一个本地项目配置主文件夹和辅助文件夹。主文件夹用于新会话、Git 操作及自动发现项目配置，辅助文件夹可用于搜索、读取和编辑。但官方资料没有确认：当系统目录和员工目录仍位于同一父 Git 仓库、或员工目录被加入辅助文件夹后，Desktop 是否一定不会再次聚合员工会话。因此“双目录”目前是候选方向，不是已经验证的修复。

官方调研来源：

- App Server 会话列表来源过滤：<https://learn.chatgpt.com/docs/app-server#list-threads-with-pagination--filters>
- Codex 配置参考：<https://learn.chatgpt.com/docs/config-file/config-reference>
- Desktop 多文件夹本地项目说明：<https://learn.chatgpt.com/docs/changelog#codex-2026-07-23-app>
- 仓库子目录可以作为保存项目：<https://learn.chatgpt.com/docs/remote-connections#hand-off-a-chat-between-hosts>

#### 用户原话佐证

此前确认的归属原则：

> 项目里面不可以出现任何员工有关的东西。桌面版的项目会话里面就应该是项目会话，员工的会话都归到员工的项目里面。

本次对解决方法、未来 Desktop 跟进需求和双目录方向的完整意见：

> 能不能不用这个方法？还是说我们可以先不管？因为这样可能导致我们创建的，我们在Negus中创建的东西。无法被桌面版正确地读取。比如说我以后想在桌面版单独的和员工，打开员工项目进行对应会话的。一些操作或者跟进之类的。如果不管的话，其实也只是在我们这个项目中会有这个问题而已吧。甚至是说，我们可以包装两层，一层就是比如说我们整一个项目之中，有两个模块。一个模块是系统模块，一个是员工模块。那我只要打开系统模块的文件夹，作为一个项目来。然后之后GitHub推送管理的时候又用更上一层的大文件夹来进行管理。我说的可能有点抽象，我的意思就是说，我们的一个大项目。大项目文件夹里面有两个功能文件夹。第一个功能文件夹就是我们的系统。第二个功能文件夹就是我们的员工。我在CodeX桌面版打开项目的时候。我就打开我们的第一个功能文件夹进行创建项目。但是我用Git管理的时候，GitHub管理的时候，我就用我们上一层的大文件夹来进行管理。把我们员工和系统两个部分都进行管理好。然后我在桌面版打开我们的系统文件夹的时候。我也可以通过提示来让CodeX去访问到我们员工文件夹的路径，进行修改里面的内容。看一下这个方案如何？而且感觉这个也可以暂时不做。因为理论上只影响这一个项目，其他项目是不影响的，对吧？

#### 当前决定与未来方向

- **当前决定：暂缓处理。** 本轮不修改员工 `thread/start` 的来源，不重构目录，也不迁移、归档或删除现有员工 Thread。
- **暂时接受的影响：** Codex Desktop 的 negus 项目会话列表可能继续出现员工会话。只要员工 Thread 的 `cwd` 始终位于 negus 的 `employees/*`，且不改成实际工作的业务项目路径，这个显示问题当前只影响 negus 项目，不影响其他业务项目。
- **不可放松的边界：** 员工会话仍必须归员工项目；不得为了操作目标项目文件，把员工 Thread 的 `cwd` 改成目标工作项目并让污染扩散到其他项目。
- **优先评估的未来方向：** 将父级 negus Git 仓库组织为“系统模块目录 + 员工模块目录”；Git 和 GitHub 仍在父目录统一管理，Codex Desktop 只把系统模块目录保存为日常系统项目，员工项目按需单独打开。系统会话需要访问员工文件时，通过明确路径和权限访问员工模块。
- **实施前验证门槛：** 先用最小目录样例验证 Desktop 在同一父 Git 仓库下是否按保存的子目录隔离会话，并验证读取、修改兄弟目录、Git 提交、构建和部署均不受影响；验证通过后才能讨论移动现有目录。
- **备选方向：** 只有未来 negus 提供明确的“在 Desktop 打开/继续员工会话”能力后，才重新评估把员工会话标记为 `appServer` 来源。
- **重新提起时的依据：** 后续再次出现“negus 主项目混入员工会话”问题时，应先引用本节，不得再次直接提出移动员工目录、独立 `CODEX_HOME`、使用 `.gitignore` 或未经验证地隐藏全部员工 Thread。

## 文档维护规则

每次讨论并修改某项功能时，必须在同一次文档修改中记录：修改前体验、确认后的当前定义、用户操作变化、归属和数据流变化、当前未实现项、验证结果以及精确到分钟的更新时间。不得只更新代码、提交说明或历史日志。
