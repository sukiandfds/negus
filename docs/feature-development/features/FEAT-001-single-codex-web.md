---
feature_id: FEAT-001
title: 单人 Codex Web 对话与控制
status: implemented_pending_review
current_version: v0.10.12
last_updated: 2026-09-22 21:57 +08:00
owners: [conversations, execution, web_ui]
key_paths:
  - web-ui/src/features/conversations
  - web-ui/src/features/execution
  - windows/server/conversation-service.mjs
  - windows/server/app-server-conversation-store.mjs
  - windows/server/execution-tracker.mjs
  - windows/server/context-management-service.mjs
  - web-ui/src/features/context-management
  - web-ui/src/features/models
---

# FEAT-001：单人 Codex Web 对话与控制

## 当前快照

2026-09-25 02:27 +08:00 BUG-20260925-01 / FEAT-001 (handoff_pending): 修复切换对话时正文和执行状态混用、旧发送回执和队列请求覆盖新对话。模型应用请求按 Thread 隔离，发送准备期切换对话时不把消息改投新对话。消息合并和显示复用同一顺序函数，同轮优先原生条目顺序，删除显示层十分钟排序限制和重复循环；备用分页保留已有助手时间。未新增依赖或改写历史记录。内存定向回归、3项历史测试及前端构建通过；未重启、未提交，浏览器实际场景和存量串话记录未验收。

- 2026-09-22 21:57 +08:00：底部栏的会话改为打开最近一条会话，按最新助手回复时间排序，并跳过桌面助手会话；没有其他会话时仍打开桌面那条。正在归档时会回到未归档列表。前端构建通过，未重启服务。
- 2026-09-22 21:30 +08:00：输入框下方增加底部栏，可在桌面、会话和群聊之间切换。单聊与群聊共用该栏，并沿用现有视图切换。手机安全区改由底部栏承担。前端构建通过，未重启服务。
- 2026-09-22 19:10 +08:00：桌面首页先稳住布局。输入区不再等连接完成后才出现；任务顺序在刷新前不重排，自动化行高固定；用量、上下文和运行状态占住固定宽度，避免上下左右抖动。前端构建通过，未重启服务。

- 2026-09-22 18:33 +08:00：同轮追加指令后，旧回复直接显示正文，只在连续旧回复的第一条上方保留「追加指令前的回复」。单张图片按原始比例显示，不再撑成正方形空白。前端构建通过，未重启服务。

- 2026-09-22 17:57 +08:00：定时任务消息按桌面 Codex 渲染。用户侧去掉 heartbeat XML，显示「由已安排任务发送」，说明默认折叠为两行；助手侧只显示结果文字，无需通知且没有正文时显示安静完成。前端构建通过，未重启服务。

- 2026-09-22 17:40 +08:00：侧边栏显示并按最新助手回复时间排序。列表不再直接使用 Codex 线程 `updatedAt`；能读到 rollout 时取最后一条最终助手回复的时间。没有助手回复的会话仍用原来的线程时间。定向测试通过，服务未重启。

- 2026-09-18 14:01 +08:00：上下文入口紧凑显示 k 数值与百分比，例如“42k / 258k（16%）”；自动压缩百分比及对应 k 数放入展开菜单，70%/80%/90% 按 Codex 当前会话实际 `modelContextWindow` 动态换算。模型切换时清空旧用量和窗口，等待新 Token 事件，避免沿用上一个模型数据。自动压缩改用真实 Token 数比较阈值，显示百分比四舍五入不影响触发。上下文定向测试 `5/5`、前端构建、后端语法和 diff 检查通过；未重启服务，手机布局待验收。

- 2026-09-18 11:14 +08:00：补齐 Codex `request_user_input` 互动。此前 app-server 客户端收到带 ID 的补充提问后立即回复空对象，网页无法显示；现在按运行实例暂存请求，通过 SSE 和恢复接口交给对应会话，弹窗支持选项、说明、其他回答和秘密输入，提交后只回复原请求并继续 Turn。普通会话、外部模型渠道及员工直属单聊共用该链路；服务重启后不保留失效请求。命令/文件/权限审批、MCP 通用表单和群聊互动不在本次范围。50 项定向测试和前端构建通过，未重启服务，真实手机交互待验收。

- 2026-09-18 00:07 +08:00：修复空会话跨渠道读取 list_turns 错误；空上下文不额外发起生成，已有上下文使用所属渠道 JSONL 兼容读取。Grok 4.5/4.6 实测接受 low/medium/high，开放等级选择并在新会话传递。修复同轮追加指令后的时间与排序，折叠追加前回复、标注真实中断、不删除消息。25 项定向测试、构建和实际问题会话顺序核对通过，计划安全重启后手机验收；原基线及未提交状态不变。

- 2026-09-17 23:39 +08:00：普通会话增加 Fusheng Grok 4.5/4.6，跨渠道确认后同项目新建会话并发送完整文字上下文，原会话保留；同渠道原地切换。渠道与模型写入 runtime/conversation-provider-routes.json，侧栏显示已知模型标识。超长历史明确拒绝，附件不自动转交，Grok 分组倍率未知时不借用 GPT 倍率。新增 provider-conversation-store.mjs 复用既有隔离渠道，员工归属保持原样。33 项定向测试通过，Grok 4.6 本机 Codex 临时会话真实调用完成；前端构建通过，后台未重启、手机完整体验待验收。基线 0353352ba24e6e18a040437e5ba4bb28a1cef805，未提交。

- 2026-09-17 22:57 +08:00：新增状态面板“检查模型”密码输入弹窗，只查询固定 Fusheng 渠道的模型列表，不保存 Key，不自动切换摘要模型。前端构建及后台语法检查通过；手机弹窗尚未实测，接口等待后台重启。

- 2026-09-17 22:46 +08:00：修复每日进度摘要凭据读取，支持供应商配置和 env_key；固定使用 gpt-5.6-terra，不回退到主聊天模型。更新失败保留旧内容并展示提示，日期分界文案改为“每日 04:00 切换日期”。保留原布局和按需触发；未增加桌面或摘要持久化。基线 0353352ba24e6e18a040437e5ba4bb28a1cef805。

- 2026-09-17：修正手机功能切换按钮高于侧栏遮罩的问题；归档切换加入 180ms 合并及双列表缓存，取消旧请求和重试；模型设置采用更新接口返回值、丢弃过期读取，记录浏览器模型偏好供新会话使用。前端构建、定向 diff 检查通过，本机首页引用新构建；未重启服务，手机连点、真实模型调用及跨设备行为未实测。基线 `0353352ba24e6e18a040437e5ba4bb28a1cef805`，未提交。

- 2026-09-16 23:43 +08:00：按手机截图整理输入区内部间距和层级，保持外层位置、宽度及两行工具布局。模型下拉箭头紧邻文字，模型/上下文采用一致字号和颜色；减弱发送按钮及阴影，群聊同步间距。仅 CSS，手机视觉效果待确认。

- 2026-09-16 23:30 +08:00：输入区工具箱由重复打开并聚焦改为展开/收起切换，增加外部点击、Esc 关闭及展开状态；中文输入法选词不触发菜单命令。统一单聊/群聊附件、工具箱、发送按钮圆角及菜单配色，保留位置布局。共享菜单显式允许点击，修复群聊父容器 pointer-events 继承问题；群聊选人后关闭选人面板。`pnpm build:ui` 通过，手机体验待确认；未重启服务。

- 2026-09-16 23:21 +08:00：轻量标题修复（基线 `0353352ba24e6e18a040437e5ba4bb28a1cef805`）。此前生成失败显示未命名；现在优先手动名称、本地自动概括、首条消息预览，空会话使用短编号。历史列表不额外请求模型；生成等待从 20 秒增至 60 秒，并处理等待期间的 Promise 拒绝。保持本地标题归属，不修改桌面会话，不增加定时任务。会话控制及 JSONL 定向测试 21 项通过；真实模型生成和手机效果未验收，待服务重启生效。

- 用户可以在浏览器读取当前项目的真实 Codex/Happy 会话、选择对话、加载较早消息并发送指令。
- Web 发起的任务使用 Codex app-server；任务运行中可以追加引导并停止。
- 助手增量、中文状态和执行活动通过 SSE 到达页面，不使用固定频率轮询刷新整个会话目录。
- 手机页面使用窄屏布局；电脑和 iPad 保留侧栏与主对话布局。
- 输入框底部显示真实模型和实时上下文占用；支持手动压缩及按对话设置自动压缩阈值。
- 侧栏可以新建当前项目的真实 Codex 对话；模型控件读取本机目录并切换当前对话后续 Turn 使用的模型。
- 2026-07-28 手机尺寸定向检查确认：已有快照时约 264ms 显示旧对话，后台读取期间显示同步提示，完成后自动消失，不进入空白加载状态。
- 已加入事件游标重连、历史断档识别和权威状态/会话快照补偿；任务长期没有有效过程事件时，网页会主动校正状态，不再只依赖用户重开页面。
- 2026-07-28 实测：Web 任务运行中切换 VPN/Tunnel 线路会取消实时请求，并可能让 Turn 停止产出、tracker 保持 `active/unknown`、模型和会话接口同时超时；只恢复 Tunnel 不能恢复该 Turn，最终需要重启 `9360` 服务。
- 当前状态不是完整替代 Codex Desktop；代码级恢复已完成，但仍需在原开发电脑用真实长任务验证 1–2 分钟停更问题是否消失。
- 2026-08-04 用户实机确认产品提交 `63e8f948b8058d8236e7a4672b5c1f8bdbf8eb70` 出现 P0 回归；后续修复已提交并继续处理恢复、队列、最终消息交接和页面稳定性。

## 用户可见结果

用户在手机或浏览器中可以看到真实对话、Codex 是否运行、当前活动和助手回复，并能继续发送内容。网页断线重连、从后台恢复或漏掉部分事件时，会依据事件游标、执行状态和会话快照补齐；是否彻底解决真实 1–2 分钟停更仍待原开发电脑验证。Web 追加到 Thread 的内容也不保证立即出现在 Codex Desktop 当前已经打开的页面中。

## 目标与边界

目标：让用户离开电脑后仍能查看一个项目中的真实 Codex 对话并继续控制任务。

当前不解决：

- Codex Desktop 当前窗口对 Web 外部事件的热刷新；
- 多人正式权限和同时控制同一个 Turn 的仲裁；
- 修改 Codex JSONL 来伪造客户端同步；
- 尚未由 app-server 提供或尚未验证可稳定取得的 Codex Desktop 内部事件和审批 UI。

## 架构与数据链路

```text
Web UI
  -> /api/sessions, /api/session
  -> conversation-service
      -> app-server store (发送和实时执行主路径)
      -> JSONL store (历史读取与回退)

Codex app-server protocol
  -> execution-tracker
  -> realtime-hub
  -> /events SSE
  -> useConversationEvents / useCodexExecution
```

重要事实：Thread 的持久化记录、Web app-server 连接和 Codex Desktop 当前 UI 状态是三个层次，不能只因为 `threadId` 相同就视为同一实时客户端。

## 开发计划与关键决策

- 保留现有 Codex 风格 UI，内容、执行状态和附件分别放入功能目录。
- 历史读取采用分页、缓存和增量事件，避免每次刷新全量扫描。
- 发送优先走 app-server 协议，JSONL 只作为历史数据来源和受限回退，不直接改写。
- 用户运行中追加内容时使用 `turn/steer`，没有活动 Turn 时使用 `turn/start`。
- 当前连接状态以 SSE 为准，执行活动以服务端 tracker 为准，不把浏览器局部计时当成权威状态。
- 空闲和输入阶段不以实时连接状态禁用输入框；短暂断线保持静默，用户点击发送时再确认服务端是否可用。
- 页面保持轻量通知连接以发现其他设备启动的任务；只有真实 Turn 运行时启用 10 秒心跳和 25 秒失联恢复。
- 助手增量按约 100 毫秒合并渲染，不使用 20 秒整页刷新，也不按每个 token 触发一次 React 更新。
- 页面重新可见、网络恢复或实时连接重建后，主动补取当前会话和执行状态。
- 手机保留当前会话最近消息的受控快照；重新打开时先显示快照，再并行同步会话目录和当前对话。
- 过程区只显示有公开摘要的分析活动，命令隐藏 PowerShell 启动外壳，最多保留当前项和最近三项有效记录。
- 文件修改、测试、构建和命令执行都属于 Codex 原有过程事件的同步范围；不为“正在修改”另建一套独立任务状态。

## 问题记录

| 问题编号 | 分类 | 状态 | 问题 | 根因与正确路径 |
| --- | --- | --- | --- | --- |
| `FEAT-001-I01` | 特例 | resolved | 对话标题曾显示正文而不是真实标题 | 解析层混淆标题来源；标题必须读取会话元数据，找不到时明确回退，不能拿正文冒充 |
| `FEAT-001-I02` | 特例 | mitigated | 长对话和会话列表加载慢，手机重新打开时需要等待 | 固定长度读 JSONL 首行、重复扫描和目录轮询；已增加分页、语义事件、受控浏览器快照和列表/当前对话并行同步，服务端长 Thread 仍需后续继续优化 |
| `FEAT-001-I03` | 特例 | active | Web 消息不在 Desktop 当前页面实时出现 | 独立客户端连接与 UI 状态不共享；转交 `FEAT-005`，不能在 React 层伪修复 |
| `FEAT-001-I04` | 特例 | resolved | 运行中不能输入、状态不一致、计时跨 Turn | 前端禁用输入且 tracker 没按 Turn 重置；已接入 steer、服务端状态和按 Turn 时间 |
| `FEAT-001-I05` | 普适 | active | 接入真实内容时曾误改 UI 和组件边界 | 见 `PROC-001`，后续内容/功能修改不得顺带重做 UI |
| `FEAT-001-I06` | 特例 | resolved | 输入框聚焦时出现浏览器蓝色外框 | Composer 自身缺少完整的原生 focus/appearance 清理；已限制在输入组件内修复 |
| `FEAT-001-I07` | 特例 | resolved | Web 缺少 Desktop 的上下文占用与压缩能力 | 已接入 app-server 的真实 token usage、当前模型、`thread/compact/start` 和完成事件；不按消息数量估算 |
| `FEAT-001-I08` | 特例 | implemented_pending_review | 用户实测任务运行约 1–2 分钟后经常不再更新过程，只有稍后重新打开页面才发现已经完成 | 已补上 `lastEventId` 重连、服务端重放、历史断档识别和权威快照校正；v0.10.4 修复前端恢复仍读取旧 tracker、并在 `active` 时跳过最终会话同步的问题。代码检查已完成，真实手机长任务验收前不标记 resolved |
| `FEAT-001-I09` | 特例 | resolved | Web 只能继续已有对话，不能新建真实 Codex Thread 或切换模型 | 已按本机 app-server `0.146.0-alpha.3.1` 协议接入 `thread/start`、`model/list` 和 `thread/settings/update`；功能按 conversations/models 模块归属拆分 |
| `FEAT-001-I10` | 普适 | mitigated | 发送请求超时可能被页面误判为失败，重复发送又可能启动重复任务 | 后端和前端在失败后确认新 Turn ID；确认已启动则继续跟踪，无法确认则恢复输入且不自动重发；运行中 steer 暂不做猜测性确认 |
| `FEAT-001-I11` | 特例 | implemented_pending_review | Web 此前不能处理 Codex 内部审批和补充提问 | `request_user_input` 已按 Codex 原协议接入弹窗、恢复和定向回传，等待手机真实验收；命令、文件和权限审批仍未提供网页交互，不能与 Windows UAC 混为一类 |
| `FEAT-001-I12` | 普适 | planned_p0 | 项目服务可能出现端口仍在线、但 Turn 长时间没有真实事件且核心对话接口全部无响应的半失效状态；2026-07-29 在没有已确认网络切换的情况下再次发生 | 需要开发防卡死与受控恢复机制；必须同时检测真实业务接口、Turn 最后事件时间和任务收口事件，不能把 tracker 的查询更新时间当成真实进度。确认连续异常后只恢复项目服务，并保持端口、Token、当前公网入口和会话数据不变；具体方案仍需重启频率限制、原因记录和明确的页面状态 |
| `FEAT-001-I13` | 特例 | implemented_pending_review | 过程区重复显示空洞的“已完成分析”，命令详情只看到冗长 PowerShell 路径 | 已接入 Codex 公开的 reasoning summary；无内容的分析记录不再保留，PowerShell 命令只展示实际执行部分，过程区限制为最近四项，等待用户体验 |
| `FEAT-001-I14` | 普适 | planned_p0 | 手机 UI 排查过程触发 Windows 网络授权弹窗并卡住；用户被迫回到电脑确认，手机残留“正在处理” | UI 诊断工具也必须经过远程无交互安全门；系统弹窗阻塞、取消或超时后必须清理运行状态。后续重启是恢复措施，不是事故原因 |
| `FEAT-001-I15` | 特例 | implemented_pending_review | 手机版开始约 2 分钟基本能持续收到实时进度，之后不再更新过程，用户无法判断任务是否仍在运行 | SSE 心跳、事件序号重放和断档恢复已存在；v0.10.4 进一步在无真实进展、页面恢复和重连时强制查询 Codex 权威状态并补拉当前会话。仍需手机真实长任务确认两分钟后是否持续恢复，不能用整页轮询代替 |
| `FEAT-001-I16` | 特例 | implemented_pending_review | 创建新对话后，新对话短暂出现随即消失，无法继续使用 | 已确认 `thread/start` 成功后首次用户消息前不会进入 `thread/list`；会话目录现保留尚未收录的新 Thread，并忽略过期列表响应，等待用户体验确认 |
| `FEAT-001-I17` | 特例 | active | 发送新消息时，有概率暂时显示上一条消息的处理过程 | 后续按 Turn 隔离过程状态；新发送立即建立独立状态，旧 Turn 内容不得进入新 Turn，真实 `turnId` 返回后再完成绑定 |
| `FEAT-001-I18` | 功能缺口 | implemented_pending_review | 模型选择区域没有思考程度选择 | 已接入模型支持的 reasoning effort，并与模型选择合并为“模型 · 思考程度”入口；等待用户体验 |
| `FEAT-001-I19` | 特例 | implemented_pending_review | 手机输入框的上下文百分比无法直接看出实际 Token 数和压缩点 | 右下角同时显示当前 k 数、运行时窗口、百分比和阈值 k 数；切换模型不沿用旧容量，设置仍保存百分比并按当前窗口动态换算。等待手机宽度与可读性验收 |
| `FEAT-001-I20` | 特例 | resolved | 助手通过内部图片工具展示图片时，最终回复只有文字；改用 Markdown 本地图片后，手机端又按原始宽度溢出，比例看起来错误 | JSONL 解析暂存工具输出中的图片并合并到下一条最终助手回复，主 app-server 会话只补充媒体块；Markdown 和结构化图片共用响应式容器。真实问题会话已确认返回 4 张图片 |
| `FEAT-001-I21` | 普适 | mitigated | 页面长时间只显示“正在运行”且没有新内容，用户无法判断 Codex 正常处理、等待安全重启还是已经卡死 | 已对照 Codex 官方 app-server 与 Happy 源码，采用“实时事件负责快路径、权威状态和持久化会话负责恢复”的边界；v0.10.4 已修复“最终回复已保存但页面仍卡在运行中”。服务端核心接口半失效仍由 `I12` 处理，不能继续靠状态文案或单一超时猜测 |
| `FEAT-001-I22` | 特例 | implemented_pending_review | 新建对话在首条消息前无法选择模型或思考程度 | `thread/start` 后尚无 rollout，旧逻辑错误调用 `thread/resume` 并返回 `no rollout found`；现复用新 Thread 的运行上下文，首条消息成功后再恢复历史 Thread 逻辑 |
| `FEAT-001-I23` | 功能增强 | implemented_pending_review | 用户和助手消息缺少时间；过程区默认展开且完成用时没有使用 Codex 权威字段 | 消息现按本地时区显示 `MM-DD HH:mm`，跨年显示 `YYYY-MM-DD HH:mm`；过程区默认折叠，完成用时读取 `turn/completed.durationMs`，等待用户体验确认 |
| `FEAT-001-I24` | 普适 | active | 跨设备实时会话在结束、重新打开和流式回答交接时状态不一致：页面重开不刷新仍显示旧的“处理中”；最终回答快结束时已显示的流式文字短暂消失；新消息开始后短暂串出上一次 Turn 的思考过程 | 根因待通过真实事件序列进一步确认，初步怀疑是恢复快照、流式草稿和最终持久化消息之间缺少同一 `runId/turnId/eventSeq` 的单调交接。正确路径是以权威 Run 生命周期和终止事件为准，流式内容只追加不回退；页面恢复先清理旧运行态并补拉终态快照；新 Turn 建立独立事件缓冲，禁止旧 Turn 事件进入新消息。关联 `FEAT-001-I08`、`I15`、`I17`、`FEAT-005` |
| `FEAT-001-I25` | P0 回归 | implemented_pending_review | 产品提交 `63e8f948b8058d8236e7a4672b5c1f8bdbf8eb70` 后，用户实机观察到消息顺序被旧内容替换、新发送消息不能立即跨 Web 设备显示、最终光标持续闪烁、同一助手答案重复显示 | 已让 `submissionId` 同时成为本机乐观消息和 SSE 消息的稳定身份；会话请求使用返回时的最新缓存并拒绝旧 `contentVersion`；正式答案按 Turn、Item 或终态内容完成流式交接，未交接草稿在终态转为静态。Windows 测试 `84/84`、生产构建、语法和补丁检查通过；服务未重启，真实跨设备效果待验收 |
| `FEAT-001-I26` | 特例 | implemented_pending_review | 会话来回切换时定位不准，切回来后可能跳到不对应的位置 | 绝对 `scrollTop` 无法抵抗虚拟消息高度变化，且切换瞬间旧滚动监听可能记录错线程；现按可见消息锚点和相对偏移恢复，找不到锚点时才回退到像素位置；生产构建已通过，运行态仍待验收 |

## 版本时间线

### 2026-07-21 | v0.1.0 | prototype

- 计划：先让浏览器展示单个项目中的真实对话。
- 实际：形成基础会话列表和内容展示 Demo。
- 偏差：标题和内容结构仍有误判，加载方式偏全量。
- 问题：`FEAT-001-I01`、`FEAT-001-I02`。
- 验证：原始记录见 `../../records/DEVELOPMENT_BUG_LOG_2026-07-21.md`。
- Git：历史提交见仓库日志。

### 2026-07-22 | v0.2.0 | implemented

- 计划：接入真实 JSONL 内容并优化加载性能。
- 实际：完成真实会话、结构化内容、分页和功能目录拆分。
- 偏差：发现数据接入过程中曾误动 UI，重新明确 UI/内容/功能边界。
- 问题：`FEAT-001-I01`、`FEAT-001-I02`、`PROC-001`。
- 验证：见 `../../records/DEVELOPMENT_LOG_2026-07-22.md`。
- Git：`8cf1e3e` 包含后续交互控制与同步边界记录。

### 2026-07-22 | v0.3.0 | implemented_pending_review

- 计划：让浏览器不仅能看，还能真实启动 Codex 工作。
- 实际：接入 app-server 发送、执行 tracker 和 SSE 状态。
- 偏差：Web 与 Desktop 当前窗口不是同一个运行实例，产生连续性问题。
- 问题：`FEAT-001-I03`、`FEAT-001-I04`。
- 验证：构建和基础接口通过；详细记录见 `../../records/DEVELOPMENT_LOG_2026-07-22.md`。
- Git：`8cf1e3e`。

### 2026-07-25 22:49 +08:00 | v0.4.0 | implemented_pending_review

- 计划：修复手机单人对话的发送、附件、状态、助手消息框和实时体验。
- 实际：完成运行中引导、停止、实时状态、附件入口、消息流和移动端体验修复。
- 偏差：Desktop/Web 连续性仍属于架构问题，没有在本轮伪装解决。
- 问题：`FEAT-001-I03` 保持 active；`FEAT-001-I04` resolved。
- 验证：`pnpm build:ui`、定向测试和真实服务接口通过。
- 用户可见变化：手机可继续控制任务并看到更明确的实时状态与助手回复。
- Git：`5a8ad71`。

### 2026-07-27 00:18 +08:00 | v0.5.0 | implemented_pending_review

- 计划：补齐 Desktop 风格的模型和上下文管理，并修复输入框蓝色外框。
- 实际：发送按钮左侧显示真实模型、实时上下文占用、压缩按钮和自动阈值；运行中触发时排队到当前 Turn 结束。
- 边界：压缩使用 Codex app-server 原生能力，不修改 JSONL；当前模型只显示，不允许 Web 切换。
- 验证：上下文服务定向测试 2 条通过，原执行测试 3 条通过，`pnpm build:ui` 通过。
- 用户可见变化：无需猜测剩余上下文，达到阈值可自动压缩，也能随时点击手动压缩。

### 2026-07-27 10:14 +08:00 | v0.6.0 | implemented_pending_review

- 计划：修复手机和电脑浏览器无法实时更新、停留在本地发送状态以及重连打断输入的问题。
- 用户体验顺序：用户消息立即出现；服务端真实开始后显示运行状态；助手回复按小段逐步出现；完成后同步最终记录。
- 空闲边界：输入不依赖服务器连接，短暂重连不提示、不禁用输入；页面重新可见时同步一次。
- 运行边界：真实 Turn 活跃时使用 10 秒心跳，25 秒没有事件或心跳时重建连接，恢复后补取会话和状态。
- 性能边界：不做 20 秒全量刷新；增量文本约每 100 毫秒合并一次，降低手机重渲染频率。
- 修改范围：仅实时通信、会话状态、执行状态和 Composer 功能逻辑；不修改视觉样式，不扩展群聊与 Desktop 同步。
- 实际：SSE 增加代理刷新、事件序号和短期重放；服务端状态快照保存当前助手增量；页面在重连、重新可见和恢复联网时补拉会话与状态。
- 实际：空闲断线保持静默且不禁用输入；运行阶段 10 秒心跳，首事件 8 秒、后续事件 25 秒超时恢复；回复按 100 毫秒合并渲染。
- 验证：实时中心、执行状态和上下文定向测试共 7 条通过；后端语法、`git diff --check` 和 `pnpm build:ui` 通过。

### 2026-07-27 20:36 +08:00 | v0.7.0 | implemented_uncommitted

- 计划：在不动群聊、Desktop 同步和公网入口的前提下，收尾单人 Codex 的新对话、真实模型切换和发送失败恢复。
- 实际：侧栏接入真实 `thread/start`；新增独立 models 功能模块，通过 `model/list` 展示本机模型并用 `thread/settings/update` 切换当前 Thread 后续 Turn 的模型。
- 实际：发送请求异常后只确认是否出现新的真实 Turn；已启动则继续跟踪，未确认则恢复输入，不自动重复提交。
- 附件：未改协议；现有图片、音频和普通文件映射及上传恢复测试继续通过。
- 遇到的问题：官方 Codex manual helper 返回 HTTP 403，未继续重复尝试；改用本机最新 Codex 运行时生成的 experimental JSON Schema 核对准确方法与参数。`Get-NetTCPConnection` 因本机 WMI 不可用，按 `PROC-002` 改用 `netstat.exe`。
- 验证：14 条定向测试、后端语法、`git diff --check` 和 `pnpm build:ui` 通过；真实 `/api/models` 返回 5 个可用模型。为避免污染用户会话，没有在实机验证中额外创建测试 Thread。
- 服务：仅启动 `9360` 项目服务；Cloudflare Tunnel PID `28632` 保持未变。
- 待确认：新对话按钮、模型下拉和手机实时更新的具体交互效果由用户体验；运行中 steer 超时仍选择报错并恢复输入，不做无法证明的重复提交或成功判定。

### 2026-07-28 00:06 +08:00 | v0.7.0 | backlog_updated

- 发现：项目服务出现半失效状态；`9360` 端口和 SSE 仍存在，但 `/api/sessions` 超过 10 秒无响应，页面持续停在“正在读取对话”。
- 恢复：人工重启项目服务后，对话列表接口恢复为约 140ms 返回；本次没有重启 Tunnel。
- 判断：仅有浏览器重连和单次任务超时不足以处理服务半失效；绿色连接状态也不能只以 SSE 存活为依据。
- 待开发：新增 `FEAT-001-I12` 防卡死与受控恢复机制。先保留结果约束，具体技术方案后续评估，不在本次记录中预设实现方式。

### 2026-07-28 00:37 +08:00 | v0.8.0 | implemented_uncommitted

- 计划：快速解决手机重新打开慢、同步失败后空白等待，以及过程区空洞“已完成分析”和不可读命令路径。
- 实际：浏览器保存当前会话最近 60 条、最多约 768KB 的受控快照；打开时先显示快照，再并行同步会话目录和当前对话。读取 12 秒超时后保留旧内容，并在 10 秒后自动重试一次。
- 实际：接入 `item/reasoning/summaryTextDelta`；没有摘要的分析不写入过程列表，PowerShell 命令移除启动器外壳，过程记录限制为当前项和最近三项。
- 边界：未处理桌面端任务状态，未开发服务自动重启，未修改基础 UI，未展示隐藏思维链。
- 验证：`execution-tracker` 6 项定向测试通过；后端语法、`pnpm build:ui` 通过；等待最终 `git diff --check` 和用户手机体验。
- Git：uncommitted，未提交、未推送。

### 2026-07-28 01:02 +08:00 | v0.8.1 | implemented_uncommitted

- 计划：检查 v0.8.0 的手机缓存同步和过程内容功能，发现真实错误时做最小修复。
- 实际：使用本机 Edge、`390x844` 视口和独立浏览器上下文模拟手机重新打开；预置真实会话快照，并把 `/api/session`、`/api/sessions` 延迟 3 秒。页面约 264ms 显示缓存，同步提示在请求期间存在，完成后消失，没有出现空白读取状态，控制台无应用错误。
- 实际：没有发现需要保留的产品代码修复；检查期间因测试脚本误判加入的两处临时代码变化已完整撤回，没有把无依据的复杂逻辑留在项目中。
- 偏差：PowerShell 向 Node 管道传递中文断言时把字符转换为问号，最初把正常同步提示误判为缺失；旧页面实时请求还干扰了 reload 场景。最终改用 ASCII Unicode 转义和“关闭旧上下文、在新上下文预置快照”的检查方式。
- 问题：关联 `PROC-002`、`PROC-005`、`PROC-010`、`PROC-012`；本轮没有新增重复问题编号。
- 验证：`execution-tracker` 6 项测试通过；`pnpm build:ui` 和 `git diff --check` 通过；两张手机检查截图仅保存在系统临时目录，没有写入仓库。
- 用户可见变化：相较 v0.8.0 没有新增行为；本轮确认“先显示缓存、后台同步、同步完成后收起提示”的既有实现正常工作。
- Git：uncommitted，未提交、未推送。

### 2026-07-28 09:15 +08:00 | v0.8.1 | incident_recorded

- 发现：用户发送手机 UI 截图，要求排查 PWA 内容缺失和页面横向拖动；助手处理这条 UI 排查任务时触发 Windows 网络授权弹窗并卡住。
- 影响：用户通过手机无法继续，只能回到电脑前处理弹窗；页面继续显示“正在处理任务”。弹窗处理后，用户才发送“检查重启服务”，服务随后从 PID `14936` 重启为 PID `33708`。
- 判断：事故发生在 UI 排查阶段；后续重启是恢复措施，不是事故原因。问题同时涉及诊断工具的远程无交互边界和任务中断后的状态清理。
- 待开发：`FEAT-001-I14` 要求远程 UI 检查默认限于源码、已有截图和静态产物；可能启动浏览器、Node、网络监听或触发系统授权的工具必须先通过无交互安全门。被弹窗阻塞后必须清理残留任务状态。
- 纠正：本条最初把弹窗错误归因于后续服务重启，已于 2026-07-28 09:53 +08:00 按截图和用户说明修正。

### 2026-07-28 09:23 +08:00 | v0.8.1 | bug_recorded

- 用户实测：手机版在任务开始后的前约 2 分钟基本能看到实时进度，之后过程更新停止，无法判断任务是否仍在运行。
- 当前判断边界：只确认用户可见现象，尚未确认是 SSE 断开、代理超时、心跳恢复失效、事件遗漏还是手机浏览器后台策略。
- 待排查：新增 `FEAT-001-I15`，后续只针对连接续期、心跳、事件序号恢复和后台节流做定向检查，不以高频整页轮询替代实时链路。
- 本次边界：只登记问题，没有修改代码、检查服务或执行重启。

### 2026-07-28 10:02 +08:00 | v0.8.1 | backlog_updated

- 新增 `FEAT-001-I16`：创建新对话后短暂出现又消失，当前无法稳定创建并继续使用。
- 新增 `FEAT-001-I17`：新消息有概率暂时复用上一轮处理过程，存在 Turn 状态串线。
- 新增 `FEAT-001-I18`：现有模型选择缺少思考程度设置。
- 合并既有 `FEAT-001-I15`：手机版前约 2 分钟有实时进度，之后不再更新过程。
- 开发顺序：先修实时链路，再修新对话和 Turn 隔离，最后补齐思考程度选择。
- 本次边界：只更新后续开发记录，没有修改代码、UI 或服务。

### 2026-07-28 15:47 +08:00 | v0.8.2 | backlog_updated

- 用户实测推翻了“持续同步已基本解决”的判断：任务约 1–2 分钟后仍经常停止更新，重开页面后才看到完成。
- 将 `FEAT-001-I08` 从 `mitigated` 恢复为 `active`，后续以真实长任务为验收依据。
- 明确文件修改、测试和构建提示属于 Codex 过程事件同步，不新增独立的“正在修改”产品需求。
- 本轮只更新需求和状态，没有修改代码。

### 2026-07-28 17:43 +08:00 | v0.9.0 | implemented_pending_review

- 计划：修复任务运行约 1–2 分钟后过程停更、只能重开页面确认结果的问题。
- 实际：网页重连携带最后事件编号；服务端重放短期历史，并明确报告历史截断或服务重启造成的断档；页面在断档、重连、重新可见或活跃任务长期无有效进度时，以执行状态和会话快照校正。
- 边界：没有新增独立“正在修改”状态，也没有实现服务自动重启、Desktop 同任务检测或群聊停止/追加。
- 验证：全量 Node 测试 `42/42` 通过，TypeScript 与 Vite 生产构建通过；真实原开发电脑长任务、断网和后台恢复仍待晚上验证。
- 用户可见变化：正常情况下继续显示 Codex 原有过程；事件丢失或连接恢复时自动补齐，不要求用户主动重开页面。
- Git：与本次实现同一提交。

### 2026-07-28 23:19 +08:00 | v0.9.1 | incident_recorded

- 现象：网页任务在修改两个文件后停在“状态暂时无法确认”，没有完成后续检查或最终回复。
- 证据：对应 Turn 最后真实记录停在 23:13:54，没有完成事件；tracker 仍为 `active/unknown`；本地 `/api/models` 和 `/api/sessions` 均在 8 秒后超时。
- 触发条件：任务运行期间先后切换 VPN 节点并重连 Tunnel，Tunnel 日志记录多条进行中请求被 `context canceled`。
- 恢复：仅重启 `9360` 项目服务，PID 从 `2632` 变为 `29660`；香港 Tunnel PID `33596`、固定域名、Token、会话数据和未提交文件修改均保留。
- 验证：重启后模型接口 `200/0.003s`、会话接口 `200/0.265s`、公网版本接口 `200/1.34s`。
- 问题：`FEAT-001-I12` 再次发生；关联 `FEAT-006-I08`、`PROC-018`。
- 用户可见变化：旧僵死状态被清除，网页可以重新连接；原任务不会自动续跑，需要基于已保留修改重新发送继续指令。
- Git：`uncommitted`，未提交、未推送。

### 2026-07-28 23:24 +08:00 | v0.10.0 | implemented_pending_review

- 计划：解决手机输入框设置按钮拥挤、两个百分比含义不清、压缩图标难以理解和字体不统一的问题。
- 实际：模型和思考程度合并为一个入口；当前上下文占用保留为独立入口；自动压缩阈值和“立即压缩”移入上下文二级菜单。
- 交互：主界面显示“模型 · 思考程度”和“上下文 49%”；菜单内使用“模型”“思考程度”“当前使用”“立即压缩”“自动压缩阈值”等明确文字。
- 样式：运行状态和两个设置入口统一为 `12px`、相同字重和点击高度；手机继续保持两行输入区，不增加第三行。
- 问题：`FEAT-001-I18` 更新为 implemented_pending_review；新增 `FEAT-001-I19`，等待手机体验后决定是否 resolved。
- 验证：`pnpm build:ui` 和 `git diff --check` 通过；按用户约束未做视觉验收。
- 用户可见变化：输入框不再直接出现无说明的 `80%` 和压缩图标，低频设置集中到文字清晰的二级菜单。
- Git：`uncommitted`，未提交、未推送。

### 2026-07-29 20:43 +08:00 | v0.10.1 | p0_incident_recorded

- 用户指令：Web Turn 根据既定范围准备开发用户态 Tunnel 自动恢复；不动正式 Windows 服务，VPN 切换和断网恢复时静默检查，不监听电脑唤醒。
- 时间线：Turn `019fadd4-ab07-7c61-a43b-1ec5ed0c7677` 于 20:23:43 开始；20:24:58 完成最后一条计划任务查询；至 20:34:01 之间没有可见事件，随后只写入一次内部分析，没有后续命令、回复、`task_complete` 或 `turn_aborted`。
- 用户可见现象：页面持续显示“正在分析任务”，最后停在已经完成的 `schtasks` 查询，无法判断任务是否仍在运行。
- 服务证据：20:36:54 `/api/execution-status` 等待 10 秒超时；随后 `/api/models` 和 `/api/sessions` 均在 5 秒后超时；端口 `9360` 仍由旧 PID `32680` 监听。
- 根因边界：已确认最后一条命令本身没有卡住；已确认 tracker 的 `updatedAt` 会被状态校正请求刷新，不能代表真实工作进展。尚未定位 app-server 内部阻塞点，不能把大文件读取、计划任务查询或某个内部锁写成确定根因。
- 实际产出：该 Turn 只完成现有脚本、文档和计划任务检查，没有修改文件、构建或最终交付。
- 恢复：仅停止旧 PID `32680` 并通过 `pnpm start:demo` 启动新 PID `35024`；未重启 Tunnel，未处理工作区未提交改动。
- 验证：重启后本地模型接口 `200/34ms`，固定公网版本接口 `200/2241ms`。
- 后续约束：防卡死判断必须以“核心接口连续失败 + Turn 长时间无真实事件 + 缺少完成/中断事件”为依据；恢复后明确告知旧 Turn 不会自动继续。关联 `PROC-019`。
- Git：`uncommitted`，未提交、未推送。

### 2026-07-29 22:39 +08:00 | v0.10.2 | implemented_pending_review

- 计划：借鉴 OpenClaw 常驻 Supervisor 与 Harness Run 账本机制，使任务恢复不依赖手机浏览器，并解决完整回复出现后状态仍长时间停留在运行中的问题。
- 实际：`app-server-conversation-store` 在真实 Run 长时间无事件时由后台主动探测；连续失败时只重建 Codex app-server 子进程。`execution-tracker` 持久化最近运行状态，服务重启后把未完成 Run 标记为中断；完整回复完成后进入“正在收尾”，后台确认 Thread idle 后结束。
- 事故：首次加载新后端时，助手在 Web Turn 内串联停止与启动 `9360`；停止服务同时中断了命令自身，启动没有执行，公网返回 `502`。该问题登记为 `PROC-020`。
- 修复：端口停止后单独执行 `pnpm start:demo` 恢复 PID `38876`；新增 `pnpm restart:demo`，通过独立 Worker 等待当前 Run 收口后再重启，禁止 Web Turn 自杀式 Stop/Start。
- 验证：Supervisor、收尾状态和运行账本共 18 项定向测试通过；PowerShell 与后端语法、`pnpm build:ui`、`git diff --check` 通过；本地版本、模型和固定公网版本接口均返回 `200`。安全重启的第二次真实演练暂未执行。
- 用户可见变化：手机关闭或休眠时后台仍监管任务；回复文本完成后状态会明确进入收尾并由后台确认结束；以后执行安全重启时先完成当前回复，再短暂断线并自动恢复固定链接。
- Git：`uncommitted`，未提交、未推送。

### 2026-07-29 23:15 +08:00 | v0.10.3 | implemented_pending_review

- 用户问题：助手第一次声称展示了 4 张图片，但手机没有图片；第二次通过本地 Markdown 路径展示后，手机只看到图片局部，比例错误。
- 根因一：4 张图片只存在于 `custom_tool_call_output` 的 `input_image` 中，最终 `agentMessage` 只有文字；主会话读取不会自动包含内部工具媒体。
- 根因二：ReactMarkdown 的裸 `<img>` 没有使用结构化图片的响应式容器，原始 `960 x 1280` 图片在窄屏中横向溢出。
- 实现：JSONL Store 暂存工具媒体并合并到下一条最终助手消息；Conversation Service 在保持 app-server 文本和顺序的前提下补充 JSONL 媒体；两种图片统一使用可点击原图的响应式容器。
- 验证：4 项定向测试、`pnpm build:ui` 和 `git diff --check` 通过；真实会话接口返回 4 张图片，本地和固定公网接口均为 `200`。
- 过程问题：任务从 23:05:35 持续到 23:15:16；补丁因中文编码匹配失败一次，真实会话检查又因中文字符串匹配误判产生额外检查。安全重启 Worker 等待 Run 收口，23:15:21 才启动新服务。页面期间只有笼统的“正在运行”，无法区分真实执行、静默阶段和等待重启。
- 待开发：新增 `FEAT-001-I21`。下一步不得继续叠加状态文案、超时或重启补丁，必须先从已有调研索引核对 Happy、OpenClaw/Harness、Orca 等项目的真实运行状态机制。
- Git：随 2026-07-29 晚间整理提交。

### 2026-07-31 15:31 +08:00 | v0.10.4 | implemented_pending_review

- 用户问题：网页版和手机版可能长期停在“回复中/正在运行”，但 Codex 实际已经完成；只有关闭并重新打开页面后，最终回复才出现。用户要求先核对 Codex、Happy、豆包等行业做法，禁止继续叠加猜测性的超时和状态文案。
- 行业核对：Codex 官方 app-server 以 `turn/completed` 作为 Turn 终态，并通过 `thread/read.status` 提供可重新查询的 Thread 状态；Happy 对实时事件使用消息序号快路径，断线恢复或序号不连续时重新拉取持久化状态。豆包既有报告只用于交互参考，其登录后通信协议未经验证，不作为技术事实。
- 已确认根因：`recoverRealtime` 发现长时间无进展后仍调用未开启 `reconcile` 的 tracker 快照；只要旧快照仍为 `active`，代码就提前返回，不再同步当前会话。因此最终回复已经持久化时，页面仍可能一直显示运行中，而重新打开页面会因重新读取会话而看到结果。
- 实现：页面重连、恢复可见、事件断档或长时间无真实进展时，强制查询 Codex 权威状态；无论权威状态仍为运行中还是已经结束，都补拉当前会话。发送确认失败后的恢复也改为权威校正，仍不自动重复提交。
- 体验补充：文字发送超过 3 秒且尚未进入真实运行状态时显示“网络较慢”；用户停止任务后先显示“正在停止”，无法确认时显示“停止状态待确认”；任务结束后自动折叠分析和命令过程，不再持续铺开“已完成分析”。
- 边界：不修改基础 UI 样式、公网入口、服务生命周期、群聊逻辑或 Desktop/Web 同步。Desktop 当前页面不热刷新仍归 `FEAT-001-I03` / `FEAT-005`；端口在线但核心接口无响应仍归 `FEAT-001-I12`。
- 过程问题：Codex 官方手册入口在当前网络返回 HTTP 403；本轮没有重复修改 MCP 或全局配置，改用 OpenAI 官方 GitHub app-server 源码和 Happy 官方仓库完成核对。
- 验证：执行状态、会话控制和 SSE 恢复共 23 项定向测试通过；`pnpm build:ui` 与 `git diff --check` 通过。按当前任务边界未启动或重启服务，也未做视觉验收；真实手机长任务仍由用户验收。
- 用户可见变化：即使完成事件偶发遗漏，页面也会通过真实 Thread 状态和持久化会话补出最终回复，不再要求关闭重开；弱网发送和停止过程拥有明确但不夸大的状态。
- Git：`uncommitted`，未提交、未推送。

### 2026-08-01 12:41 +08:00 | v0.10.5 | implemented_pending_review

- 用户问题：点击“新话题”后空白对话短暂出现，随即被旧历史列表覆盖并自动切回旧对话。
- 根因：`thread/start` 已成功，但 Codex 在首条用户消息前尚未生成 rollout，新 Thread 暂时不会出现在 `thread/list`；前端创建后立即刷新列表，把这个合法的过渡状态误判为会话消失。
- 实现：会话目录在当前页面生命周期内保留尚未进入服务端列表的新 Thread；列表刷新会将其与正式历史合并，服务端出现相同 Thread ID 后自动转为正式记录；并忽略晚到的旧列表响应。
- 边界：没有修改基础 UI、Codex 协议、服务端、新话题标题策略或空话题跨页面刷新持久化。
- 验证：`pnpm build:ui` 和 `git diff --check` 通过；未启动或重启服务，真实交互由用户验收。
- 用户可见变化：点击“新话题”后会稳定停留并可立即输入；后台列表更新不会切回旧对话；首条消息被 Codex 收录后正常进入历史列表。
- Git：`uncommitted`，未提交、未推送。

### 2026-08-01 16:20 +08:00 | v0.10.6 | implemented_pending_review

- 用户问题：新建对话已经可以停留，但首条消息前无法选择模型。
- 真实错误：运行记录显示新 Thread 在模型操作期间返回 `no rollout found for thread id ...`。
- 根因：`thread/start` 已把新 Thread 加载到当前 app-server，但旧逻辑仍先调用面向历史会话的 `thread/resume`；首条消息前没有 rollout，因此操作失败。
- 实现：仅对刚创建且尚未发送首条消息的 Thread 复用 `thread/start` 返回的模型上下文；模型和思考程度修改直接写入该 Thread；首条消息成功后移除过渡状态，继续使用原有恢复逻辑。
- 验证：会话控制定向测试 `5/5`、后端语法、`pnpm build:ui` 和 `git diff --check` 通过。
- 用户可见变化：新建话题后无需先发送消息，即可立即选择模型和思考程度。
- Git：`uncommitted`，未提交、未推送。

### 2026-08-01 23:23 +08:00 | v0.10.7 | implemented_pending_review

- 用户要求：所有用户与助手消息显示月、日、24 小时时间，精确到分钟，跨年时增加年份；思考过程默认折叠，可点击查看过程和用时。
- 时间来源：app-server 使用 Turn 的 `startedAt` / `completedAt`；JSONL 降级读取使用每条记录原始 `timestamp`；前端即时发送消息先使用本机当前时间，服务端同步后由真实记录替换。
- 展示格式：当年 `MM-DD HH:mm`，跨年 `YYYY-MM-DD HH:mm`，不使用中文日期。
- 展示位置：时间位于对应消息气泡上方；用户消息气泡使用较浅的局部背景色，不修改全局主题。
- 过程与用时：沿用现有 Codex 状态文字，不新增“工作了”等文案；运行过程默认折叠；完成用时读取 `turn/completed.durationMs`，运行中才按真实开始时间动态递增。
- 当前边界：初版继续展示当前或最近一个 Turn 的过程；尚未把所有历史 Turn 的过程分别绑定到每条历史助手回答。
- 验证：消息、会话和执行状态定向测试 `22/22`、后端语法、`pnpm build:ui` 和 `git diff --check` 通过。
- Git：随本轮功能提交推送至 `codex/publish-current-panel`。

### 2026-08-02 11:39 +08:00 | v0.10.8 | implemented_pending_review

- 计划：在思考程度选择器中补充英文代码，帮助用户理解 `low`、`medium`、`max`、`xhigh` 等模型参数。
- 实际：新增模型功能域内的统一文案格式化函数；下拉选项、折叠后的当前模型状态和悬停提示均显示中英对照，例如 `中 (medium)`。
- 偏差：未修改基础 UI 样式、布局、请求协议或模型行为。
- 问题：关联 `FEAT-001-I24` / `PROC-022`；本轮只记录问题，不改变其状态。
- 验证：`pnpm build:ui` 通过；`git diff --check` 通过。
- 用户可见变化：选择思考程度时能同时看到中文含义和 Codex 使用的英文参数名。
- Git：`uncommitted`，未提交、未推送。

### 2026-08-04 09:55 +08:00 | v0.10.9 | implemented_pending_review

- 用户问题：发送消息后当前页和其他 Web 设备看不到；请求返回会把新内容替换成旧内容；回答完成后光标仍闪；同一回答显示两次。
- 根因：乐观消息与服务端提交没有共用稳定身份；会话请求使用发起前缓存合并；完整响应直接覆盖请求期间的新消息；持久化最终答案与流式草稿的确认条件过严，终态草稿仍被标记为 streaming。
- 实现：提交前生成统一 `submissionId`，服务端立即广播一次 `user_message_submitted`；前端按消息 ID 幂等追加；增量、完整刷新和旧页加载统一基于返回时最新缓存，保留未落盘消息并拒绝旧版本；最终答案按 Turn、Item 或终态内容确认，终态未交接草稿停止光标。
- 验证：Windows Node 测试 `84/84`；`pnpm build:ui`；后端语法检查；`git diff --check`；构建产物包含 `user_message_submitted`。
- 运行边界：按用户要求没有启动或重启服务；当前运行后端仍是旧代码，跨设备真实交互待后续重启后由用户验收。
- Git：`uncommitted`，未提交、未推送。

### 2026-08-06 | v0.10.11 | implemented_pending_review

- 计划：收敛刷新、重开、历史加载和内容稳定性问题，不再通过额外状态文案掩盖加载顺序。
- 实际：在 `ConversationView` 中使用可见消息锚点恢复历史加载位置；在会话和应用启动阶段等待新快照完成后再展示；异步 IndexedDB 快照也纳入加载门；加载指示器移出消息列表，避免改变列表高度。
- 偏差：本轮只同步已完成代码状态，不继续扩大到 Desktop 实时同步或新的状态系统。
- 问题：关联 `FEAT-001-I24`、`FEAT-001-I25`、`PROC-022`；真实移动端和跨端体验仍待确认。
- 验证：提交 `ddf43a6c406d010357dce7ebdc133b97de66b5b9`；此前 UI 构建和定向检查通过；本次未重新运行构建。
- 用户可见变化：刷新、重开和加载较早消息时，页面应先完成必要读取再稳定展示，阅读位置不应因历史加载反复跳动。
- Git：`ddf43a6c406d010357dce7ebdc133b97de66b5b9`。

### 2026-08-10 | uncommitted UX follow-up

- 用户问题：会话来回切换定位不准，切回来后可能跳到不对应的位置。
- 实际：`ConversationView` 保存可见虚拟项的消息锚点和相对偏移；恢复时先定位同一消息，再根据当前测量结果校正；切换瞬间的滚动事件读取当前会话引用，避免旧监听器串线程。
- 追加修复：历史 `failed`、`interrupted` 或 `systemError` 状态不再仅凭 `startedAt` 插回当前对话过程区，避免旧任务状态被误看成当前任务；当前活动过程和本次已观察到的终态仍保留。
- 边界：不改变基础 UI、消息内容、执行状态或历史加载协议；找不到原消息时保留绝对位置作为降级。
- 验证：`pnpm build:ui` 和 `git diff --check` 通过；未启动或重启服务，未做浏览器运行态验收。
- Git：`uncommitted`，未提交、未推送。

### 2026-09-18 11:14 +08:00 | v0.10.12 | implemented_pending_review

- 用户需求：Negus 先对齐 Codex，至少能渲染 `request_user_input` 互动弹窗并把答案交回原任务。
- 修改前：app-server 客户端把所有服务端请求自动回复为空对象，补充提问在到达网页前被吞掉。
- 修改后：所属运行实例暂存待回答请求；SSE 立即推送，刷新和重连通过接口恢复；桌面与手机弹窗支持选项、说明、其他答案和秘密输入，提交后只回复匹配的 Thread 和请求 ID。
- 范围：普通会话、外部模型渠道和员工直属单聊；不包括审批、MCP 通用表单和群聊内 Agent 互动。
- 验证：后端语法检查通过；会话路由、执行状态、会话控制和供应商路由定向测试 `50/50` 通过；`pnpm build:ui` 通过。未重启服务，真实弹窗及手机体验待验收。
- Git：`uncommitted`，未提交、未推送。

#### 2026-09-18 14:01 +08:00 | 上下文数值补充

- 修改前：入口只显示百分比，用户无法判断当前使用多少 Token、自动压缩实际对应多少 Token；模型切换后还可能短暂保留旧窗口。
- 修改后：入口紧凑显示当前 k 数、Codex 当前实际窗口和百分比；自动压缩百分比及压缩点在菜单内显示。模型变化后等待新运行时数据，不猜测模型理论容量。
- 逻辑修正：自动压缩直接比较 `usedTokens` 与 `contextWindow × threshold`，不使用四舍五入后的显示百分比判断。
- 验证：上下文服务测试 `5/5`、`pnpm build:ui`、后端语法和 `git diff --check` 通过；未重启服务，手机实机待验收。

## 下一步

- 第一优先验收当前 `ddf43a6c406d010357dce7ebdc133b97de66b5b9`：刷新、关闭重开、向上加载历史时先稳定后展示；用户消息和最终助手回复各只显示一次；最新消息顺序不被旧快照覆盖。
- 若上述体验仍有问题，再针对实际复现继续修复；已完成的消息幂等、队列编辑/发送、重命名、重新编辑和恢复代码不重复开发。
- 第一优先级在手机和电脑浏览器验收 `FEAT-001-I08` / `I15` / `I21`：真实任务持续运行 5 分钟以上，验证断网重连、后台切回和完成事件遗漏后是否自动补齐最终回复；失败时记录最后真实事件、权威 Thread 状态和会话是否已持久化。
- 行业状态边界已经完成源码级映射；后续不得回退为单纯增加超时或状态文案。若仍出现端口在线但核心接口无响应，直接按 `FEAT-001-I12` 排查服务半失效，不与前端事件恢复混为一类。
- 随后复核 `FEAT-001-I16` 新对话是否稳定保留、`FEAT-001-I17` 跨 Turn 过程是否仍会串线。
- `FEAT-001-I18` 思考程度选择和 `FEAT-001-I19` 手机设置菜单已实现，等待用户体验。
- 随后评估 `FEAT-001-I12`：识别端口/SSE 存活但核心接口无响应的半失效状态并安全恢复；实现前先确定监控位置、异常阈值、重启保护和状态展示。
- `FEAT-001-I14` 与防卡死方案一起评估：限制会触发桌面确认的诊断工具，并清理弹窗阻塞后的残留状态。
- Codex 补充提问已实现待验收；命令、文件和权限审批仍按 `FEAT-001-I11` 保持独立范围，当前需要时仍回到电脑确认。
- 需要正式双端连续性时转入 `FEAT-005`，先做 Connector/控制权设计。
- 不在本功能中扩展正式多人权限或群聊队列。
