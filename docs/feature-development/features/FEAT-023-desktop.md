# FEAT-023 桌面初版

- 更新时间：2026-09-20 20:54 +08:00
- 版本：v0.3.0
- 状态：handoff_pending
- 基线：0353352ba24e6e18a040437e5ba4bb28a1cef805
- 分支：codex/publish-current-panel；已有大量未提交修改，本轮保留，不提交、不推送。

## 用户确认的方向

Negus 桌面是默认入口和随时可返回的工作空间。保留熟悉的侧边栏、顶部及输入区位置，主要改变中央内容。长期支持自由定制、总助手、文件和文档操作；文件管理暂不实施。

## 本轮可见变化

2026-09-22 01:51 +08:00：跨功能排查继续。ProjectStatusControl原2.5秒轮询在慢网时叠加请求，现限制单一在途读取、页面隐藏跳过、返回立即检查、10秒超时恢复，相同结果不替换状态；保留旧内容及现有错误反馈。群聊流式切换修复详见FEAT-002。10项加载回归测试、构建通过。现有9360服务授权只读单次测量：目录200/1244ms/51244字节，自动化200/4ms/491字节，群聊快照200/1ms/32264字节；未输出凭据或正文。非前后对比/手机性能验收。目录employeeLastActivity为最近活动时间调用完整findSession路径待优化，浏览器验收工具仍不可用；无重启、提交、推送。

2026-09-22 01:43 +08:00：续查修正selectSession在复用预读请求时遗漏loading/error设置，移除adoptSelection重复设置；增加对应回归用例，现5项加载测试通过。Goal内容不变时仍独立保存本批检查时间，避免刷新后重复扫描已检查会话；空队列不触发缓存写入。pnpm build:ui通过，main-CR6xC_Hz.js。浏览器初始化再次失败，未取得视觉和真实延迟证据；群聊静态检查发现历史加载/快照缺少切换代际保护，待后续处理，目标未完成。同步PRD与功能索引；分支codex/publish-current-panel，基线651e86c9471e09bd79bbec1260f02d08750406a3，本批未提交、未推送、未重启。

2026-09-22 01:38 +08:00：全面加载闪屏排查第一批修复（目标仍在进行）。当前任务已有数据时后台Goal检查不再切换为“正在同步”；相同Goal结果不再更新React状态。目录请求增加版本保护，旧响应不能覆盖后发请求。对话读取不因切换中止，旧会话结果只进自身缓存；初次同步后延迟800ms串行预读前4条中的未缓存普通会话，不预读员工/特定conversation作用域，不发送消息。切换时立即设置正确loading/error，所有入口恢复截断缓存时强制完整页读取。功能暂时隐藏不再清空同一对话的滚动定位标识。新增4项加载测试涵盖预读不改当前会话、晚返回不串会话、截断缓存恢复及预读失败隔离。浏览器工具初始化失败（kernel assets路径缺失），尚未验证真机闪屏、读取耗时、群聊和其余功能切换；不得视为整体完成。保留当前目标，继续排查。

2026-09-22 01:11 +08:00：按用户要求隐藏iPad横屏顶部“文件、编辑、视图、帮助”模拟窗口栏，同时取消其网格占位。沿用触控抽屉断点，保留桌面标题、侧栏入口及其他顶部功能；普通鼠标宽屏保持原状。前端构建通过，未做iPad真机复验，无重启。

2026-09-22 01:09 +08:00：iPad横屏等触控设备使用手机同款抽屉对话栏，默认收起，顶部侧栏按钮呼出、点击遮罩关闭；释放桌面横向空间，保留宽屏标题栏布局。复用AppShell现有开关状态和遮罩，依据any-pointer:coarse启用（带触屏的电脑也适用），普通鼠标电脑维持常驻侧栏。TypeScript/Vite构建通过，未进行iPad真机验收；无服务重启。

2026-09-22 00:48 +08:00：按用户截图收紧定制体验。定制/完成按钮移入固定顶部栏（分享按钮左侧图标），定制状态由App传给桌面；退出、Esc、切换页面取消未完成手势。移除画布tabIndex及外框焦点样式，删除顶部模式提示和工具条占位。仅异常保留不占布局的浮动反馈。绘制层两侧各留18px滚动安全区，安全区覆盖组件边缘以保证纵向原生滚动，画布坐标及组件可放置范围保持原宽度；从中间拖动仍可落在两侧。定制时overflow-y:scroll、稳定滚动条槽和原灰色细滚动条样式；iPad原生覆盖滚动条是否持续显示未实测。TypeScript/Vite构建通过，本轮未进行浏览器触屏验收；无重启、提交或推送，保留既有改动。

### v0.4.0 定制模式与手绘区域（2026-09-22 00:12 +08:00）

- 本轮范围只到用户画出区域：没有实现内容自动开发、独立区域会话或新的恢复系统。取消撤销/重做入口与单快照恢复逻辑。保留已有内容、业务源及其他工作区修改。
- “定制桌面／完成定制”单按钮切换。定制模式禁选文字，组件点击选中、直接拖动、尺寸手柄缩放；更多菜单固定/解锁、移除、针对区域对话。普通模式不显示角部调整入口，恢复业务点击与滚动。
- 空白处连续画线，SVG笔迹逐帧显示，本地等弧长采样比较圆/矩形边界误差；松手规整为圆或长方形，不使用起终点拉框，不调用模型。点击、直线、未闭合/过小路径不创建；难辨识的闭合路径让用户选择两种形状。新区域拒绝与现有组件重叠。
- 形状作为独立字段持久化，不限制业务类型；圆形保持正圆。画完仅保存和选中，原输入框指向该区域，不自动发送消息、不弹业务类型选择、不自动聚焦键盘。内容生成技术分层另行讨论。
- pointercancel、多指介入、窗口变化/失焦取消未提交动作；固定组件不允许移动/缩放，邻居不能把它挤走。沿用逻辑网格与本地布局，不承诺像素级自由画布或跨设备同步。
- 已验证：13 项定向测试通过；TypeScript/Vite构建；隔离浏览器12类交互检查通过（原有会话为fixture）：单入口、无撤销、真实触摸圆/长方形、目标绑定、零模型消息、固定/解锁、取消手势、移动、缩放、刷新保存、手机/宽屏正圆比例。已实际查看390×844与1080×800截图，移除小空白区域多余标题避免与管理按钮挤占。未验收真实手写笔硬件、各种不规则笔迹识别率与真机帧率。
- 文件：EditableDesktop.tsx/.module.css、desktopShapes.ts、desktopLayout.ts、useDesktopWorkspace.ts；同步PRD、总产品定义、功能索引。基线0353352ba24e6e18a040437e5ba4bb28a1cef805，分支codex/publish-current-panel，保留脏工作区；无新依赖、无重启、未提交推送。handoff_pending。

2026-09-21 02:08 +08:00：按用户要求，将当前任务卡片内独占一行的“重新同步”改为标题栏刷新图标按钮；详情弹窗同样改为标题栏图标。沿用原重试逻辑，读取期间禁用，保留可访问名称和提示。TypeScript/Vite构建通过；未进行手机视觉复验，无服务重启。

### v0.3.2 直接框选与组件调整（2026-09-21 02:00 +08:00）

- 用户要求“快点改好代码我看看效果”。本轮只改现有桌面交互：常驻“框出区域”入口，手机一键进入框选，电脑鼠标可直接在空白处拖出区域；松手关联原输入框。小于 12px 的点击/误拖不创建区域，固定组件占用区拒绝创建。
- 组件长按或点击角部调整入口直接选中，显示拖动和缩放手柄；只有选中组件暂停内部点击，其他组件保持可用。可直接“说说怎么改”，管理菜单保留固定、移除。取消缩放手柄点击时跳变高度，方向键支持逐格缩放。
- 区域最小尺寸由半宽/4行降至2列/2行，已有布局与内容保留。仍为12列吸附布局，不宣称像素级自由画布。内置业务组件仍保留最低8行高度。
- 空区域不再默认推荐自动化；整块空白可点击描述需求。提交后仅按真实会话/轮次显示需求状态，不把助手回复写进区域或声称功能生成完成。任意新业务组件生成与挂载仍未接通。
- 验证：布局与消息关联定向测试9/9、TypeScript/Vite构建通过。未进行本轮触屏浏览器验收或真实模型生成验收；沿用现有服务、不重启、不提交推送。状态 handoff_pending。

### v0.3.1 首页状态表达修复（2026-09-21 01:34 +08:00）

- 正常首页直接呈现任务与状态，不重复显示不确定性免责声明。来源覆盖范围和管理说明收进自动化详情折叠区。
- 当前任务区分断线、目录读取失败、长期 Goal 同步失败；失败保留数据但标记历史快照，新增重新同步按钮，重试真实目录和失败 Goal 请求。加载或失败时不再断言“当前没有运行任务”。
- 自动化请求失败显示同步失败和点击重试、快照读取时间；部分源失败也不显示完整任务统计。通知策略明确为“仅执行失败时通知”，空运行明细显示“暂无执行明细”。不修改调度记录或启停状态，不扩大数据覆盖范围。
- 前端构建通过；本轮仅前端修改，无需服务重启。原工作区改动保留，未提交推送；手机实机体验仍由用户验收。

### v0.3.0 真实自动化组件与区域管理（2026-09-20 20:54 +08:00）

- 修正此前把“在区域创建功能”实现成“把回复贴入区域”的产品错误：停止自动将助手最终回复写入组件，针对区域对话显示目标名称/位置上下文，并在原助手浮层回复。保留原对话、输入区和已保存内容。
- 右上角第 7–12 列、第 1–8 行、包含自动化需求/说明的旧“选区 1”迁移为独立自动化组件；不更改其他区域。新空白选区可以直接选择自动化组件或针对该区域提出需求。
- 新增只读 `/api/desktop/automations`，在原认证边界内读取本机 Codex SQLite 调度记录，并合并目录内文件配置；不返回 prompt、不执行脚本、不调用模型、不创建定时器任务。沿用服务现有 Node 24，不新增依赖。Node 20 无 SQLite 模块时明确显示数据不完整。
- 组件显示真实名称、启停状态、周期、下次执行；详情支持全部/已启用/其他状态、最近触发、最多 5 条已有执行记录、通知策略、来源和关联对话。每 15 秒可见刷新、后台暂停、返回补查、缓存先展示，接口服务端合并并发查询并缓存 5 秒。读取失败保留旧数据和警告，不把失败当没有任务。已启用不等于正在运行；没有明细不虚构成功记录。范围仅本机 Codex 自动化，不含 OS 计划任务、外部服务、其他设备；暂停/恢复/计划编辑尚未接入。
- 正常和编辑模式长按约 480ms、右键或编辑区管理按钮打开菜单：针对该区域对话、调整位置与大小、固定/取消固定、从桌面移除。修复长按松手的合成点击误关菜单。原生 dialog 支持焦点约束、Esc/遮罩关闭；移动超过 8px 取消长按。
- 固定后位置/尺寸不变，内容继续更新，邻居碰撞不能把固定组件挤走。移除只改布局，不停止后台任务；普通模式也能撤销，编辑模式可重新添加自动化或当前任务。保留半宽卡片、触摸取消和窗口变化取消拖动。
- 修改入口：`web-ui/src/features/desktop/`、`App.tsx`、`windows/server/desktop-automations.mjs`、`request-handler.mjs`；复用原主题，无新的 UI/动画依赖。
- 验证：16 项定向测试通过，前端构建和后端语法检查通过；13 项隔离浏览器检查通过，涵盖旧区域迁移、真实自动化源渲染/详情、普通与编辑触摸菜单、固定解锁、拖动/撤销、框选/目标绑定、回复隔离、刷新保存、移除撤销、触摸取消和宽屏边界。浏览器会话接口为隔离测试数据，自动化数据来自真实本机只读源；未调用模型或修改任务。已查看手机 390×844 与宽屏 1080×800 截图，不等于真实手机 FPS/网络验收。
- 当前本机读到 1 项启用任务“Negus 服务与远程链接巡检”，有 last/next 时间、无 automation_runs 明细；不据此承诺其他来源没有任务。
- 已通过现有 restart-web-demo 安排当前活动 Turn 结束后安全重启，保留运行中进程的原 Token、设备名称、9360 端口和 Tunnel；旧 PID 23260，新 PID 与实际 HTTP 接口最终加载状态尚未核实，不把排队成功当作已上线。未提交或推送，保留原有脏工作区。记录同步本文件、产品定义与功能索引，状态保持 handoff_pending。

### v0.2.0 桌面编辑与选区对话（2026-09-20 14:46 +08:00）

- 此前组件固定展示；现在长按约 480ms 或点击“编辑桌面”进入编辑模式。手指移动超过 8px 取消长按，正常浏览保持纵向滚动。编辑时组件内容 inert，手柄拖动、角部调整尺寸、半宽/整宽、移除和撤销；移除不删除业务数据。已移除的当前任务可重新添加。
- 桌面采用 12 列逻辑网格、32px 行单位。坐标从桌面内容区及当前滚动位置换算；同一浏览器窗口缩放/旋转后按宽度重新布局，不按设备型号猜测坐标。网格平时隐藏，编辑显示。拖动用 rAF 更新吸附预览，不逐帧更新 React 布局/存储；松手提交，碰撞组件向下让位，支持边缘滚动。pointercancel/窗口尺寸变化取消当前拖动；键盘方向键可移动，框选区 Enter 可创建区域。
- 选择“框选”后拖出范围，松手创建内容区并关联原输入框。发送时加入该区域范围和内容要求，不新建并行会话。按 Thread、用户消息及回复轮次关联最终结果；文字、图片、文件/链接等沿用安全内容渲染，预览后选择保留或取消。后续用户消息不覆盖此前选区，失败提示保留原内容。取消选区关联不停止真实任务。
- 布局、保留内容、待处理关联使用浏览器本地缓存；刷新恢复。最多 60 个区域，保存失败明确提示。当前不跨设备同步，不执行任意生成的 HTML/JS，不具备自动部署新业务组件、语音或文件管理能力；图片审核等操作仍需对应实现。当前任务依旧读取真实状态。
- 动画沿用现有主题，提交布局使用 160ms 过渡，支持减少动态效果。拖动期间暂停任务组件的额外查询；未新增运行依赖或服务端代码。
- 验证：12 项任务/布局/选区回复绑定测试；TypeScript/Vite 构建。使用已安装 Playwright/Edge 独立上下文，API 全部隔离为测试数据，9 项浏览器检查通过：手机渲染、拖动吸附、撤销、框选关联输入框、回复预览保留、刷新恢复、触摸长按、触摸取消、宽屏边界。已查看 390×844 与 1080×800 截图。未用真实模型执行选区请求，未实测真实手机帧率、跨设备同步或与小红书的速度对比，因此保持 handoff_pending。
- 项目记录同步本文件、PRODUCT_DEFINITION.md、功能索引。分支及基线见文件头，未提交/推送/重启，保留原工作区改动。

2026-09-20 13:49 +08:00 修正：手机继续双列半宽，左右边距与列间距 8px，当前任务标题栏收紧，三条任务各固定 60px 两行（标题+短状态、单行指令）。移除小卡片项目名、状态独立行和底部放大按钮，标题栏保留放大入口，详情与列表保持。缓存 Goal 及最多 100 条已读取任务预览，返回桌面不清空查询记录；取消额外项目目录轮询及首次 SSE 重复刷新，详情按会话/轮次变化读取，不跟随流式状态每次 updatedAt 重查。缓存内容先显示，错误保留旧内容并提示待同步。构建及 6 项状态测试通过；浏览器工具仍因缺少 kernel assets 初始化失败，未取得手机视觉和真实刷新计时，不能宣称达到参考应用速度。

2026-09-20 13:37 +08:00：按参考图改为桌面双列半宽卡片，手机保持两列、紧凑排版。任务概括改为真实对话标题和用户最新指令，状态单独显示；小卡片指令显示两行，放大后显示读取内容全文。只请求当前可见任务的详情，保留员工 conversationId，失败显示未读到。该规则取代下文 v0.1.3 的 Goal objective 优先展示。两次前端构建通过，未重启；未重新进行浏览器实机验收。

v0.1.3 新增唯一的“当前任务”小组件。桌面 3 条概括和状态 → 放大概览 → 分类完整列表；任意任务可进入原会话，员工会话复用身份导航。运行任务优先，没有运行任务才显示最近明确完成项；长期 active Goal 在轮次间保留，暂停/额度用完/失败/中断独立归类。概括来自原生 Goal objective 或已有标题，不额外生成摘要。每个 Thread 去重，完成排序采用运行/Goal 更新时间而非会话改名时间。原侧边栏、输入框和回复浮层保持。

复用 useProjectDirectory 的目录及实时运行事件，补充 Goal 事件与只读查询。可见桌面每 15 秒补查目录，Goal 首次 3 路并发，活动 Goal 定期补查，无 Goal 缓存按变化或 5 分钟过期刷新；离开桌面取消查询。错误保留提示，idle 不算完成。范围为目录可读取、已接入 Negus 状态源的任务；不承诺发现任意外部进程。弹窗使用原生 dialog，支持焦点约束、Esc、关闭、遮罩关闭和手机滚动，无新增依赖或服务端改动。

此前根路径进入最近会话；现在无明确会话/群聊参数时进入桌面。指定 thread、archived 或 view=conversation/group 的链接仍进入对应页面；view=desktop 显式回桌面。顶部共用切换器增加桌面入口，手机标题预留相应空间。

v0.1.2 按用户确认清空桌面中央内容：移除欢迎文字、最近对话、项目卡片、定制按钮及空白提示，后续按具体需求添加。此前已有浏览器卡片布局不再读取，也不清除其他浏览器数据。侧边栏、顶部、底部共用输入框、回复浮层及重新展开入口保留。项目、对话和文件不删除，数据归属及桌面 Thread 绑定不变。

v0.1.0 错误地单独实现了简化输入框并在发送后跳转会话。v0.1.1 删除 DesktopComposer，桌面与单人对话直接使用同一个 ConversationComposer、同一套附件/模型/思考等级/工具箱/队列/Goal/停止/压缩处理，后续功能修改同步生效。进入桌面准备个人项目专用普通 Thread，并按项目目录在当前浏览器记住编号，后续访问复用未归档且可写的同目录 Thread；准备期间不展示旧会话操作控件。首次进入会创建空 Thread，但不会自动发消息。

发送后保持桌面，真实 ConversationView 在顶部圆角浮层中滑入并流式显示内容；继续追问接续同一个 Thread。支持收起、Esc 和重新展开，收起不停止任务、不删除历史。浮层复用原消息渲染和消息操作，保留 Codex 补充提问；底部输入框位置不变。根据输入框高度避让，触屏在剩余内容区展示。减少动态效果设置下禁用动画。桌面会话目前按浏览器保存绑定，不宣称跨设备总助手身份；切换不同 Thread 仍沿用原输入草稿生命周期。

## 范围与未实现项

### 2026-09-22 00:24 +08:00 直接成形与后台恢复

- 移除形状选择弹窗；闭合轮廓直接匹配圆形或矩形，误触和未成形笔迹不新增区域、不提问。
- 当前任务与自动化移除重复同步失败警告，保留记录并呈现更新时间/上次更新状态。目录读取错误每15秒在可见桌面自动重试；Goal与自动化沿用自动补查。部分自动化源不可读时合并保留旧记录，完整成功结果才移除不再存在的任务。
- 不修改真实业务执行失败、发送及存储错误的结果。仅前端交互与读取恢复，无后端重启；分支 codex/publish-current-panel，基线 0353352ba24e6e18a040437e5ba4bb28a1cef805，保留既有未提交工作。
- TypeScript/Vite构建及14项形状/布局测试通过。实体手机网络体验未验收，状态 handoff_pending；未提交推送。

本轮是桌面入口和当前任务小组件，不是完整总助手。跨项目主动检索、助手推荐、自动结论沉淀、偏好学习、持久桌面助手身份、自然语言任意定制、文件管理与文档编辑仍待实现。旧个人助手设计作为方向参考，不迁移其 localStorage 业务数据。

## 验证与交接

v0.1.3：6 项任务分类/去重/排序定向测试及前端构建。浏览器工具初始化报“failed to write kernel assets / 系统找不到指定的路径”，未完成桌面/手机点击验收及真实长 Goal 验收；handoff_pending。无服务重启、提交或推送。基线与分支见文件头，既有未提交修改保留；本轮记录同步本文件、产品定义和功能索引。

前端 TypeScript/Vite 构建及本轮文件 diff 检查；服务端未改动，无需重启。未进行手机/桌面浏览器视觉验收和真实模型发送验收，不宣称完整体验已验证。用户刷新后通过顶部“桌面”体验；原有会话深链不强制跳转。项目记录统一维护在本文件、产品定义与功能索引。
