# Grok 模型接入与自身服务重启事故复盘

记录时间：2026-09-22 02:21 +08:00。用户明确要求“反思写入经验”。

## 用户要求与实际损失

用户要求快速修复 Grok 入口、手动刷新模型及 Grok 4.7 推理强度，反复强调高效、敏捷、不要多余操作。助手将小范围问题拆成多轮不完整修复、解释和重启。用户明确反馈耗费约半小时，严重耽误睡觉；重启后 Negus 卡在断开状态，增加了用户的时间、精力和情绪负担。责任在助手，不能归咎于用户催促或中断工具。

## 具体错误与证据边界

1. 只确认后端按会话保存模型，就告知用户可选择 Grok，未核实前端入口；截图证明菜单遗漏 Grok。
2. 给模型菜单增加刷新按钮，却只重读本地静态模型目录，没有查询供应商新增模型，导致 Grok 4.7 刷新不出来。
3. 接入真实模型目录后未及时闭环核实推理能力，留下空选项。后续三个最短实际请求均返回 HTTP 200、completed，并分别回显 low、medium、high；此前解释与等待本可减少。该结果证明当前渠道接受这些参数，不证明三档推理质量存在差异。
4. 安全重启等待承载当前工作的任务结束，形成自等待；用户要求立即重启后，助手又直接串联停止和启动，没有先保证独立执行者能够完成启动与恢复。
5. 项目早已有 PROC-020“服务不能在自身 Turn 中直接重启”，助手没有落实已有事故约束，重复犯错，不是缺少文档。
6. 最后一次停止/启动工具返回 aborted，之后用户报告页面停留在断开状态。未取得本次新 PID 与健康检查证据，不能把此前成功重启或已排队当作此次恢复完成。

## 必须执行的经验

- 用户要求小修时，围绕实际使用闭环一次定位：入口、真实数据源、选择保存及必要能力。只做直接相关的修改与最少验证，不将每一小步都当作交付，不反复扩写和汇报。
- “刷新模型”必须检查真实渠道目录，不能用重读静态列表冒充发现新模型；刷新失败准确反馈并保留已有列表。
- 用户要求快速处理时立即收束无关任务。已经获得的明确授权不重复询问；急迫不等于可以省略保障服务恢复的关键步骤。
- 重启承载当前对话的服务，禁止在当前请求链里直接 Stop/Start。先确保独立、可观察、与当前连接生命周期无关的执行者已接管完整停止、启动和失败恢复，再停止服务。
- 用户明确要求立即重启时，不再等待当前 Turn 收口；仍须由独立执行者立即执行。不能把“立即”误解为在自身执行链中杀掉自身服务。
- 确认完整新 PID、健康接口后才能说“重启完成”；排队、Worker 存活、旧服务仍在线都不是完成证据。没有可用独立执行路径时，应明确告知限制，不先停服务。
- 出现服务中断，优先恢复用户可用性，不继续功能扩展；用户改为要求复盘时，按当前指令记录，不能擅自混入重启或代码修改。

本次仅记录经验，没有恢复服务、改业务代码、重启、提交或推送；当前可用状态未复验。
